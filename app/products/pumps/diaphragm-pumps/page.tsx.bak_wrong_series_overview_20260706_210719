import Link from "next/link";
import "./diaphragm-pump-detail.css";

import detailsJson from "@/data/products/generated/pumps/diaphragm-pumps/detail/index.json";

type DiaphragmDetail = {
  slug: string;
  title?: string;
  description?: string;
  category?: string;
};

const details = detailsJson as DiaphragmDetail[];

export const metadata = {
  title: "隔膜泵 | FOREACH",
  description:
    "恒永达隔膜泵系列包括液体隔膜泵和气液混合隔膜泵，适用于仪器内部液体输送、废液抽排、负压建立和气液混合物抽排等应用。",
};

export default function DiaphragmPumpListPage() {
  return (
    <main className="diaphragm-list-page">
      <div className="diaphragm-detail-inner">
        <nav className="diaphragm-breadcrumb" aria-label="Breadcrumb">
          <Link href="/products">产品中心</Link>
          <span>/</span>
          <Link href="/products/pumps">泵</Link>
          <span>/</span>
          <span>隔膜泵</span>
        </nav>

        <section className="diaphragm-section">
          <div className="diaphragm-kicker">Diaphragm Pumps</div>
          <h1 className="diaphragm-title">隔膜泵</h1>
          <p className="diaphragm-desc">
            恒永达隔膜泵系列适用于仪器内部液体输送、清洗液转移、废液抽排、负压建立和气液混合物抽排等流体控制场景。
          </p>
        </section>

        <section className="diaphragm-section">
          <h2 className="diaphragm-section-title">产品系列</h2>
          <div className="diaphragm-list-grid">
            {details.map((item) => (
              <Link
                className="diaphragm-list-card"
                href={`/products/pumps/diaphragm-pumps/${item.slug}`}
                key={item.slug}
              >
                <h2>{item.title}</h2>
                <p>{item.description}</p>
              </Link>
            ))}
          </div>
        </section>
      </div>
    </main>
  );
}
