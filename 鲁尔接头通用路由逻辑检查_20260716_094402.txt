鲁尔接头通用路由逻辑检查

检查时间：2026-07-16 09:44:02
本次只检查，没有修改文件，也没有运行 npm build。

============================================================
文件：app\products\[category]\[slug]\[seriesSlug]\page.tsx
============================================================

------------------------------------------------------------
行号：10
命中：notFound,

上文：
    import type {
      ComponentType,
    } from "react";
    
    import type {
      Metadata,
    } from "next";
    
    import {

>>>   notFound,

下文：
    } from "next/navigation";
    
    import {
      Suspense,
    } from "react";
    
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";
    import ProductDetailClient from "@/components/products/detail/ProductDetailClient";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    
    import {
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    
    import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";
    
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";

------------------------------------------------------------
行号：18
命中：import ProductDetailClient from "@/components/products/detail/ProductDetailClient";

上文：
    
    import {
      notFound,
    } from "next/navigation";
    
    import {
      Suspense,
    } from "react";
    
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";

>>> import ProductDetailClient from "@/components/products/detail/ProductDetailClient";

下文：
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    
    import {
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    
    import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";
    
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";
    
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {

------------------------------------------------------------
行号：26
命中：import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";

上文：
    
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";
    import ProductDetailClient from "@/components/products/detail/ProductDetailClient";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    
    import {
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    

>>> import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";

下文：
    
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";
    
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{
        category: string;
        slug: string;
        seriesSlug: string;
      }>;
    };
    
    type FittingDetailRecord = {

------------------------------------------------------------
行号：28
命中：import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";

上文：
    import ProductDetailClient from "@/components/products/detail/ProductDetailClient";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    
    import {
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    
    import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";
    

>>> import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";

下文：
    
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{
        category: string;
        slug: string;
        seriesSlug: string;
      }>;
    };
    
    type FittingDetailRecord = {
      slug: string;
      model: string;

------------------------------------------------------------
行号：30
命中：import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";

上文：
    
    import {
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    
    import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";
    
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";
    

>>> import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";

下文：
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{
        category: string;
        slug: string;
        seriesSlug: string;
      }>;
    };
    
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;

------------------------------------------------------------
行号：32
命中：import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";

上文：
      getSeriesRouteParams,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    
    import hardTubeDetailsJson from "@/data/products/generated/fittings/hard-tube-fittings/detail/index.json";
    
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";
    
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    

>>> import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";

下文：
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{
        category: string;
        slug: string;
        seriesSlug: string;
      }>;
    };
    
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;
      name?: string;
      description?: string;

------------------------------------------------------------
行号：38
命中：category: string;

上文：
    import threadToBarbedDetailsJson from "@/data/products/generated/fittings/thread-to-barbed-fittings/detail/index.json";
    
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{

>>>     category: string;

下文：
        slug: string;
        seriesSlug: string;
      }>;
    };
    
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;
      name?: string;
      description?: string;
    
      sourceType?: string;
      category?: string;
      categoryId?: string;
      productTypeId?: string;
      productTypeName?: string;

------------------------------------------------------------
行号：40
命中：seriesSlug: string;

上文：
    import luerDetailsJson from "@/data/products/generated/fittings/luer-fittings/detail/index.json";
    
    import femaleThreadDetailsJson from "@/data/products/generated/fittings/female-thread-adapters/detail/index.json";
    
    import "../../../products.css";
    
    type ProductsSeriesRoutePageProps = {
      params: Promise<{
        category: string;
        slug: string;

>>>     seriesSlug: string;

下文：
      }>;
    };
    
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;
      name?: string;
      description?: string;
    
      sourceType?: string;
      category?: string;
      categoryId?: string;
      productTypeId?: string;
      productTypeName?: string;
    
      advantages?: string[];

------------------------------------------------------------
行号：53
命中：category?: string;

上文：
    
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;
      name?: string;
      description?: string;
    
      sourceType?: string;

>>>   category?: string;

下文：
      categoryId?: string;
      productTypeId?: string;
      productTypeName?: string;
    
      advantages?: string[];
      commonApplications?: string[];
    
      mainImage?: string;
      image?: string;
      heroImage?: string;
      imageCard?: string;
      additionalImages?: string[];
    
      specs?: Array<{
        label: string;
        value: string;
      }>;
    

------------------------------------------------------------
行号：54
命中：categoryId?: string;

上文：
    type FittingDetailRecord = {
      slug: string;
      model: string;
    
      title?: string;
      name?: string;
      description?: string;
    
      sourceType?: string;
      category?: string;

>>>   categoryId?: string;

下文：
      productTypeId?: string;
      productTypeName?: string;
    
      advantages?: string[];
      commonApplications?: string[];
    
      mainImage?: string;
      image?: string;
      heroImage?: string;
      imageCard?: string;
      additionalImages?: string[];
    
      specs?: Array<{
        label: string;
        value: string;
      }>;
    
      faqs?: Array<{

------------------------------------------------------------
行号：92
命中：hardTubeDetailsJson as FittingDetailRecord[];

上文：
      [key: string]: unknown;
    };
    
    type ResolvedFittingDetail = {
      detail: FittingDetailRecord;
      productTypeId: string;
      fallbackName: string;
    };
    
    const hardTubeDetails =

>>>   hardTubeDetailsJson as FittingDetailRecord[];

下文：
    
    const threadToBarbedDetails =
      threadToBarbedDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_START */
    
    const luerDetails =
      luerDetailsJson as FittingDetailRecord[];
    
    const femaleThreadDetails =
      femaleThreadDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_END */
    
    const ProductDetailView =
      ProductDetailClient as unknown as ComponentType<{
        data: any;
      }>;

------------------------------------------------------------
行号：95
命中：threadToBarbedDetailsJson as FittingDetailRecord[];

上文：
    type ResolvedFittingDetail = {
      detail: FittingDetailRecord;
      productTypeId: string;
      fallbackName: string;
    };
    
    const hardTubeDetails =
      hardTubeDetailsJson as FittingDetailRecord[];
    
    const threadToBarbedDetails =

>>>   threadToBarbedDetailsJson as FittingDetailRecord[];

下文：
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_START */
    
    const luerDetails =
      luerDetailsJson as FittingDetailRecord[];
    
    const femaleThreadDetails =
      femaleThreadDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_END */
    
    const ProductDetailView =
      ProductDetailClient as unknown as ComponentType<{
        data: any;
      }>;
    
    export const dynamicParams =
      false;

------------------------------------------------------------
行号：100
命中：luerDetailsJson as FittingDetailRecord[];

上文：
    
    const hardTubeDetails =
      hardTubeDetailsJson as FittingDetailRecord[];
    
    const threadToBarbedDetails =
      threadToBarbedDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_START */
    
    const luerDetails =

>>>   luerDetailsJson as FittingDetailRecord[];

下文：
    
    const femaleThreadDetails =
      femaleThreadDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_END */
    
    const ProductDetailView =
      ProductDetailClient as unknown as ComponentType<{
        data: any;
      }>;
    
    export const dynamicParams =
      false;
    
    function normalizeSegment(
      value: unknown
    ) {
      return (

------------------------------------------------------------
行号：103
命中：femaleThreadDetailsJson as FittingDetailRecord[];

上文：
    
    const threadToBarbedDetails =
      threadToBarbedDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_START */
    
    const luerDetails =
      luerDetailsJson as FittingDetailRecord[];
    
    const femaleThreadDetails =

>>>   femaleThreadDetailsJson as FittingDetailRecord[];

下文：
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_END */
    
    const ProductDetailView =
      ProductDetailClient as unknown as ComponentType<{
        data: any;
      }>;
    
    export const dynamicParams =
      false;
    
    function normalizeSegment(
      value: unknown
    ) {
      return (
        String(value || "")
          .trim()
          .toLowerCase()

------------------------------------------------------------
行号：108
命中：ProductDetailClient as unknown as ComponentType<{

上文：
    
    const luerDetails =
      luerDetailsJson as FittingDetailRecord[];
    
    const femaleThreadDetails =
      femaleThreadDetailsJson as FittingDetailRecord[];
    
    /* LUER_FEMALE_DETAIL_COLLECTIONS_END */
    
    const ProductDetailView =

>>>   ProductDetailClient as unknown as ComponentType<{

下文：
        data: any;
      }>;
    
    export const dynamicParams =
      false;
    
    function normalizeSegment(
      value: unknown
    ) {
      return (
        String(value || "")
          .trim()
          .toLowerCase()
          .split("/")
          .filter(Boolean)
          .pop() || ""
      );
    }

------------------------------------------------------------
行号：130
命中：seriesSlug: string

上文：
          .trim()
          .toLowerCase()
          .split("/")
          .filter(Boolean)
          .pop() || ""
      );
    }
    
    function findDetailInCollection(
      details: FittingDetailRecord[],

>>>   seriesSlug: string

下文：
    ) {
      const targetSlug =
        normalizeSegment(seriesSlug);
    
      return (
        details.find(
          (item) =>
            normalizeSegment(
              item.slug
            ) === targetSlug
        ) || null
      );
    }
    
    function findFittingDetail(
      category: string,
      slug: string,
      seriesSlug: string

------------------------------------------------------------
行号：133
命中：normalizeSegment(seriesSlug);

上文：
          .filter(Boolean)
          .pop() || ""
      );
    }
    
    function findDetailInCollection(
      details: FittingDetailRecord[],
      seriesSlug: string
    ) {
      const targetSlug =

>>>     normalizeSegment(seriesSlug);

下文：
    
      return (
        details.find(
          (item) =>
            normalizeSegment(
              item.slug
            ) === targetSlug
        ) || null
      );
    }
    
    function findFittingDetail(
      category: string,
      slug: string,
      seriesSlug: string
    ): ResolvedFittingDetail | null {
      if (category !== "fittings") {
        return null;

------------------------------------------------------------
行号：146
命中：category: string,

上文：
        details.find(
          (item) =>
            normalizeSegment(
              item.slug
            ) === targetSlug
        ) || null
      );
    }
    
    function findFittingDetail(

>>>   category: string,

下文：
      slug: string,
      seriesSlug: string
    ): ResolvedFittingDetail | null {
      if (category !== "fittings") {
        return null;
      }
    
      if (
        slug ===
        "hard-tube-fittings"
      ) {
        const detail =
          findDetailInCollection(
            hardTubeDetails,
            seriesSlug
          );
    
        return detail

------------------------------------------------------------
行号：148
命中：seriesSlug: string

上文：
            normalizeSegment(
              item.slug
            ) === targetSlug
        ) || null
      );
    }
    
    function findFittingDetail(
      category: string,
      slug: string,

>>>   seriesSlug: string

下文：
    ): ResolvedFittingDetail | null {
      if (category !== "fittings") {
        return null;
      }
    
      if (
        slug ===
        "hard-tube-fittings"
      ) {
        const detail =
          findDetailInCollection(
            hardTubeDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,

------------------------------------------------------------
行号：150
命中：if (category !== "fittings") {

上文：
            ) === targetSlug
        ) || null
      );
    }
    
    function findFittingDetail(
      category: string,
      slug: string,
      seriesSlug: string
    ): ResolvedFittingDetail | null {

>>>   if (category !== "fittings") {

下文：
        return null;
      }
    
      if (
        slug ===
        "hard-tube-fittings"
      ) {
        const detail =
          findDetailInCollection(
            hardTubeDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "hard-tube-fittings",

------------------------------------------------------------
行号：161
命中：seriesSlug

上文：
        return null;
      }
    
      if (
        slug ===
        "hard-tube-fittings"
      ) {
        const detail =
          findDetailInCollection(
            hardTubeDetails,

>>>         seriesSlug

下文：
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "hard-tube-fittings",
              fallbackName:
                "硬管接头",
            }
          : null;
      }
    
      if (
        slug ===
        "thread-to-barbed-fittings"
      ) {
        const detail =

------------------------------------------------------------
行号：177
命中："thread-to-barbed-fittings"

上文：
              productTypeId:
                "hard-tube-fittings",
              fallbackName:
                "硬管接头",
            }
          : null;
      }
    
      if (
        slug ===

>>>     "thread-to-barbed-fittings"

下文：
      ) {
        const detail =
          findDetailInCollection(
            threadToBarbedDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "thread-to-barbed-fittings",
              fallbackName:
                "螺纹转倒刺接头",
            }
          : null;
      }
    

------------------------------------------------------------
行号：182
命中：seriesSlug

上文：
          : null;
      }
    
      if (
        slug ===
        "thread-to-barbed-fittings"
      ) {
        const detail =
          findDetailInCollection(
            threadToBarbedDetails,

>>>         seriesSlug

下文：
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "thread-to-barbed-fittings",
              fallbackName:
                "螺纹转倒刺接头",
            }
          : null;
      }
    
      /* LUER_FEMALE_DETAIL_RESOLVE_START */
    
      if (
        category ===
          "fittings" &&

------------------------------------------------------------
行号：189
命中："thread-to-barbed-fittings",

上文：
        const detail =
          findDetailInCollection(
            threadToBarbedDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:

>>>             "thread-to-barbed-fittings",

下文：
              fallbackName:
                "螺纹转倒刺接头",
            }
          : null;
      }
    
      /* LUER_FEMALE_DETAIL_RESOLVE_START */
    
      if (
        category ===
          "fittings" &&
        slug ===
          "luer-fittings"
      ) {
        const detail =
          findDetailInCollection(
            luerDetails,
            seriesSlug

------------------------------------------------------------
行号：199
命中：category ===

上文：
                "thread-to-barbed-fittings",
              fallbackName:
                "螺纹转倒刺接头",
            }
          : null;
      }
    
      /* LUER_FEMALE_DETAIL_RESOLVE_START */
    
      if (

>>>     category ===

下文：
          "fittings" &&
        slug ===
          "luer-fittings"
      ) {
        const detail =
          findDetailInCollection(
            luerDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "luer-fittings",
              fallbackName:
                "鲁尔接头",
            }

------------------------------------------------------------
行号：202
命中："luer-fittings"

上文：
            }
          : null;
      }
    
      /* LUER_FEMALE_DETAIL_RESOLVE_START */
    
      if (
        category ===
          "fittings" &&
        slug ===

>>>       "luer-fittings"

下文：
      ) {
        const detail =
          findDetailInCollection(
            luerDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "luer-fittings",
              fallbackName:
                "鲁尔接头",
            }
          : null;
      }
    

------------------------------------------------------------
行号：207
命中：seriesSlug

上文：
    
      if (
        category ===
          "fittings" &&
        slug ===
          "luer-fittings"
      ) {
        const detail =
          findDetailInCollection(
            luerDetails,

>>>         seriesSlug

下文：
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "luer-fittings",
              fallbackName:
                "鲁尔接头",
            }
          : null;
      }
    
      if (
        category ===
          "fittings" &&
        slug ===
          "female-thread-adapters"

------------------------------------------------------------
行号：214
命中："luer-fittings",

上文：
        const detail =
          findDetailInCollection(
            luerDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:

>>>             "luer-fittings",

下文：
              fallbackName:
                "鲁尔接头",
            }
          : null;
      }
    
      if (
        category ===
          "fittings" &&
        slug ===
          "female-thread-adapters"
      ) {
        const detail =
          findDetailInCollection(
            femaleThreadDetails,
            seriesSlug
          );
    

------------------------------------------------------------
行号：222
命中：category ===

上文：
              detail,
              productTypeId:
                "luer-fittings",
              fallbackName:
                "鲁尔接头",
            }
          : null;
      }
    
      if (

>>>     category ===

下文：
          "fittings" &&
        slug ===
          "female-thread-adapters"
      ) {
        const detail =
          findDetailInCollection(
            femaleThreadDetails,
            seriesSlug
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "female-thread-adapters",
              fallbackName:
                "内螺纹互转接头",
            }

------------------------------------------------------------
行号：230
命中：seriesSlug

上文：
    
      if (
        category ===
          "fittings" &&
        slug ===
          "female-thread-adapters"
      ) {
        const detail =
          findDetailInCollection(
            femaleThreadDetails,

>>>         seriesSlug

下文：
          );
    
        return detail
          ? {
              detail,
              productTypeId:
                "female-thread-adapters",
              fallbackName:
                "内螺纹互转接头",
            }
          : null;
      }
    
      /* LUER_FEMALE_DETAIL_RESOLVE_END */
    
      return null;
    }
    

------------------------------------------------------------
行号：272
命中：category:

上文：
        detail.heroImage ||
        detail.imageCard ||
        "";
    
      return {
        ...detail,
    
        sourceType:
          "fitting-detail",
    

>>>     category:

下文：
          "fittings",
    
        categoryId:
          "fittings",
    
        categoryLabel:
          "接头系列",
    
        productTypeId,
    
        productTypeName:
          detail.productTypeName ||
          detail.name ||
          fallbackName,
    
        slug:
          detail.slug,
    

------------------------------------------------------------
行号：275
命中：categoryId:

上文：
    
      return {
        ...detail,
    
        sourceType:
          "fitting-detail",
    
        category:
          "fittings",
    

>>>     categoryId:

下文：
          "fittings",
    
        categoryLabel:
          "接头系列",
    
        productTypeId,
    
        productTypeName:
          detail.productTypeName ||
          detail.name ||
          fallbackName,
    
        slug:
          detail.slug,
    
        model:
          detail.model,
    

------------------------------------------------------------
行号：278
命中：categoryLabel:

上文：
    
        sourceType:
          "fitting-detail",
    
        category:
          "fittings",
    
        categoryId:
          "fittings",
    

>>>     categoryLabel:

下文：
          "接头系列",
    
        productTypeId,
    
        productTypeName:
          detail.productTypeName ||
          detail.name ||
          fallbackName,
    
        slug:
          detail.slug,
    
        model:
          detail.model,
    
        name:
          detail.name ||
          detail.title ||

------------------------------------------------------------
行号：377
命中：detailHref:

上文：
    
        showDatasheetRequest:
          false,
    
        showDrawingRequest:
          true,
    
        show3DRequest:
          false,
    

>>>     detailHref:

下文：
          `/products/fittings/${productTypeId}/${detail.slug}`,
    
        href:
          `/products/fittings/${productTypeId}/${detail.slug}`,
    
        selectionHref:
          `/products/fittings/${productTypeId}`,
      };
    }
    
    export function generateStaticParams() {
      const existingSeriesParams =
        getSeriesRouteParams();
    
      const hardTubeParams =
        hardTubeDetails.map(
          (detail) => ({
            category:

------------------------------------------------------------
行号：388
命中：export function generateStaticParams() {

上文：
          `/products/fittings/${productTypeId}/${detail.slug}`,
    
        href:
          `/products/fittings/${productTypeId}/${detail.slug}`,
    
        selectionHref:
          `/products/fittings/${productTypeId}`,
      };
    }
    

>>> export function generateStaticParams() {

下文：
      const existingSeriesParams =
        getSeriesRouteParams();
    
      const hardTubeParams =
        hardTubeDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "hard-tube-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );

------------------------------------------------------------
行号：395
命中：category:

上文：
      };
    }
    
    export function generateStaticParams() {
      const existingSeriesParams =
        getSeriesRouteParams();
    
      const hardTubeParams =
        hardTubeDetails.map(
          (detail) => ({

>>>         category:

下文：
              "fittings",
    
            slug:
              "hard-tube-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const threadToBarbedParams =
        threadToBarbedDetails.map(
          (detail) => ({
            category:
              "fittings",
    

------------------------------------------------------------
行号：401
命中：seriesSlug:

上文：
    
      const hardTubeParams =
        hardTubeDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "hard-tube-fittings",
    

>>>         seriesSlug:

下文：
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const threadToBarbedParams =
        threadToBarbedDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "thread-to-barbed-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug

------------------------------------------------------------
行号：411
命中：category:

上文：
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const threadToBarbedParams =
        threadToBarbedDetails.map(
          (detail) => ({

>>>         category:

下文：
              "fittings",
    
            slug:
              "thread-to-barbed-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const luerParams =
        luerDetails.map(
          (detail) => ({
            category:
              "fittings",
    

------------------------------------------------------------
行号：415
命中："thread-to-barbed-fittings",

上文：
          })
        );
    
      const threadToBarbedParams =
        threadToBarbedDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:

>>>           "thread-to-barbed-fittings",

下文：
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const luerParams =
        luerDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "luer-fittings",
    
            seriesSlug:

------------------------------------------------------------
行号：417
命中：seriesSlug:

上文：
    
      const threadToBarbedParams =
        threadToBarbedDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "thread-to-barbed-fittings",
    

>>>         seriesSlug:

下文：
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const luerParams =
        luerDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "luer-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug

------------------------------------------------------------
行号：427
命中：category:

上文：
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const luerParams =
        luerDetails.map(
          (detail) => ({

>>>         category:

下文：
              "fittings",
    
            slug:
              "luer-fittings",
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const femaleThreadParams =
        femaleThreadDetails.map(
          (detail) => ({
            category:
              "fittings",
    

------------------------------------------------------------
行号：431
命中："luer-fittings",

上文：
          })
        );
    
      const luerParams =
        luerDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:

>>>           "luer-fittings",

下文：
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const femaleThreadParams =
        femaleThreadDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "female-thread-adapters",
    
            seriesSlug:

------------------------------------------------------------
行号：433
命中：seriesSlug:

上文：
    
      const luerParams =
        luerDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "luer-fittings",
    

>>>         seriesSlug:

下文：
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const femaleThreadParams =
        femaleThreadDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "female-thread-adapters",
    
            seriesSlug:
              normalizeSegment(
                detail.slug

------------------------------------------------------------
行号：443
命中：category:

上文：
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const femaleThreadParams =
        femaleThreadDetails.map(
          (detail) => ({

>>>         category:

下文：
              "fittings",
    
            slug:
              "female-thread-adapters",
    
            seriesSlug:
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const routeMap =
        new Map<
          string,
          {
            category: string;
            slug: string;

------------------------------------------------------------
行号：449
命中：seriesSlug:

上文：
    
      const femaleThreadParams =
        femaleThreadDetails.map(
          (detail) => ({
            category:
              "fittings",
    
            slug:
              "female-thread-adapters",
    

>>>         seriesSlug:

下文：
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const routeMap =
        new Map<
          string,
          {
            category: string;
            slug: string;
            seriesSlug: string;
          }
        >();
    
      [
        ...existingSeriesParams,

------------------------------------------------------------
行号：460
命中：category: string;

上文：
              normalizeSegment(
                detail.slug
              ),
          })
        );
    
      const routeMap =
        new Map<
          string,
          {

>>>         category: string;

下文：
            slug: string;
            seriesSlug: string;
          }
        >();
    
      [
        ...existingSeriesParams,
        ...hardTubeParams,
        ...threadToBarbedParams,
        ...luerParams,
        ...femaleThreadParams,
      ]
        .filter(
          (item) =>
            Boolean(
              item &&
              item.category &&
              item.slug &&

------------------------------------------------------------
行号：462
命中：seriesSlug: string;

上文：
              ),
          })
        );
    
      const routeMap =
        new Map<
          string,
          {
            category: string;
            slug: string;

>>>         seriesSlug: string;

下文：
          }
        >();
    
      [
        ...existingSeriesParams,
        ...hardTubeParams,
        ...threadToBarbedParams,
        ...luerParams,
        ...femaleThreadParams,
      ]
        .filter(
          (item) =>
            Boolean(
              item &&
              item.category &&
              item.slug &&
              item.seriesSlug
            )

------------------------------------------------------------
行号：477
命中：item.category &&

上文：
        ...existingSeriesParams,
        ...hardTubeParams,
        ...threadToBarbedParams,
        ...luerParams,
        ...femaleThreadParams,
      ]
        .filter(
          (item) =>
            Boolean(
              item &&

>>>           item.category &&

下文：
              item.slug &&
              item.seriesSlug
            )
        )
        .forEach((item) => {
          const key = [
            item.category,
            item.slug,
            item.seriesSlug,
          ].join("/");
    
          routeMap.set(
            key,
            item
          );
        });
    
      return Array.from(

------------------------------------------------------------
行号：479
命中：item.seriesSlug

上文：
        ...threadToBarbedParams,
        ...luerParams,
        ...femaleThreadParams,
      ]
        .filter(
          (item) =>
            Boolean(
              item &&
              item.category &&
              item.slug &&

>>>           item.seriesSlug

下文：
            )
        )
        .forEach((item) => {
          const key = [
            item.category,
            item.slug,
            item.seriesSlug,
          ].join("/");
    
          routeMap.set(
            key,
            item
          );
        });
    
      return Array.from(
        routeMap.values()
      );

------------------------------------------------------------
行号：484
命中：item.category,

上文：
          (item) =>
            Boolean(
              item &&
              item.category &&
              item.slug &&
              item.seriesSlug
            )
        )
        .forEach((item) => {
          const key = [

>>>         item.category,

下文：
            item.slug,
            item.seriesSlug,
          ].join("/");
    
          routeMap.set(
            key,
            item
          );
        });
    
      return Array.from(
        routeMap.values()
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductsSeriesRoutePageProps): Promise<Metadata> {

------------------------------------------------------------
行号：486
命中：item.seriesSlug,

上文：
              item &&
              item.category &&
              item.slug &&
              item.seriesSlug
            )
        )
        .forEach((item) => {
          const key = [
            item.category,
            item.slug,

>>>         item.seriesSlug,

下文：
          ].join("/");
    
          routeMap.set(
            key,
            item
          );
        });
    
      return Array.from(
        routeMap.values()
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductsSeriesRoutePageProps): Promise<Metadata> {
      const {
        category,

------------------------------------------------------------
行号：504
命中：category,

上文：
    
      return Array.from(
        routeMap.values()
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductsSeriesRoutePageProps): Promise<Metadata> {
      const {

>>>     category,

下文：
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        const {
          detail,
          fallbackName,
        } = fittingDetail;
    
        return {

------------------------------------------------------------
行号：506
命中：seriesSlug,

上文：
        routeMap.values()
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductsSeriesRoutePageProps): Promise<Metadata> {
      const {
        category,
        slug,

>>>     seriesSlug,

下文：
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        const {
          detail,
          fallbackName,
        } = fittingDetail;
    
        return {
          title:
            detail.seo?.title ||

------------------------------------------------------------
行号：511
命中：category,

上文：
      params,
    }: ProductsSeriesRoutePageProps): Promise<Metadata> {
      const {
        category,
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(

>>>       category,

下文：
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        const {
          detail,
          fallbackName,
        } = fittingDetail;
    
        return {
          title:
            detail.seo?.title ||
            `${detail.model} ${
              detail.name ||
              detail.title ||
              fallbackName
            } | FOREACH`,

------------------------------------------------------------
行号：513
命中：seriesSlug

上文：
      const {
        category,
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,

>>>       seriesSlug

下文：
        );
    
      if (fittingDetail) {
        const {
          detail,
          fallbackName,
        } = fittingDetail;
    
        return {
          title:
            detail.seo?.title ||
            `${detail.model} ${
              detail.name ||
              detail.title ||
              fallbackName
            } | FOREACH`,
    
          description:

------------------------------------------------------------
行号：540
命中：category,

上文：
    
          description:
            detail.seo?.description ||
            detail.description ||
            "",
        };
      }
    
      const route =
        resolveSeriesRoute(

>>>       category,

下文：
          slug,
          seriesSlug
        );
    
      if (!route) {
        return {};
      }
    
      return {
        title:
          route.title,
    
        description:
          route.description,
      };
    }
    
    export default async function ProductsSeriesRoutePage({

------------------------------------------------------------
行号：542
命中：seriesSlug

上文：
            detail.seo?.description ||
            detail.description ||
            "",
        };
      }
    
      const route =
        resolveSeriesRoute(
          category,
          slug,

>>>       seriesSlug

下文：
        );
    
      if (!route) {
        return {};
      }
    
      return {
        title:
          route.title,
    
        description:
          route.description,
      };
    }
    
    export default async function ProductsSeriesRoutePage({
      params,
    }: ProductsSeriesRoutePageProps) {

------------------------------------------------------------
行号：562
命中：category,

上文：
    
        description:
          route.description,
      };
    }
    
    export default async function ProductsSeriesRoutePage({
      params,
    }: ProductsSeriesRoutePageProps) {
      const {

>>>     category,

下文：
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        return (
          <ProductDetailView
            data={
              toFittingClientData(
                fittingDetail
              )

------------------------------------------------------------
行号：564
命中：seriesSlug,

上文：
          route.description,
      };
    }
    
    export default async function ProductsSeriesRoutePage({
      params,
    }: ProductsSeriesRoutePageProps) {
      const {
        category,
        slug,

>>>     seriesSlug,

下文：
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        return (
          <ProductDetailView
            data={
              toFittingClientData(
                fittingDetail
              )
            }
          />

------------------------------------------------------------
行号：569
命中：category,

上文：
      params,
    }: ProductsSeriesRoutePageProps) {
      const {
        category,
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(

>>>       category,

下文：
          slug,
          seriesSlug
        );
    
      if (fittingDetail) {
        return (
          <ProductDetailView
            data={
              toFittingClientData(
                fittingDetail
              )
            }
          />
        );
      }
    
      const route =
        resolveSeriesRoute(

------------------------------------------------------------
行号：571
命中：seriesSlug

上文：
      const {
        category,
        slug,
        seriesSlug,
      } = await params;
    
      const fittingDetail =
        findFittingDetail(
          category,
          slug,

>>>       seriesSlug

下文：
        );
    
      if (fittingDetail) {
        return (
          <ProductDetailView
            data={
              toFittingClientData(
                fittingDetail
              )
            }
          />
        );
      }
    
      const route =
        resolveSeriesRoute(
          category,
          slug,

------------------------------------------------------------
行号：588
命中：category,

上文：
              toFittingClientData(
                fittingDetail
              )
            }
          />
        );
      }
    
      const route =
        resolveSeriesRoute(

>>>       category,

下文：
          slug,
          seriesSlug
        );
    
      if (!route) {
        notFound();
      }
    
      return (
        <Suspense
          fallback={
            <ProductPageSkeleton
              variant="selection"
            />
          }
        >
          <ProductSelectionClient
            locale="zh"

------------------------------------------------------------
行号：590
命中：seriesSlug

上文：
              )
            }
          />
        );
      }
    
      const route =
        resolveSeriesRoute(
          category,
          slug,

>>>       seriesSlug

下文：
        );
    
      if (!route) {
        notFound();
      }
    
      return (
        <Suspense
          fallback={
            <ProductPageSkeleton
              variant="selection"
            />
          }
        >
          <ProductSelectionClient
            locale="zh"
            initialCategoryId={
              route.categoryId

------------------------------------------------------------
行号：594
命中：notFound();

上文：
      }
    
      const route =
        resolveSeriesRoute(
          category,
          slug,
          seriesSlug
        );
    
      if (!route) {

>>>     notFound();

下文：
      }
    
      return (
        <Suspense
          fallback={
            <ProductPageSkeleton
              variant="selection"
            />
          }
        >
          <ProductSelectionClient
            locale="zh"
            initialCategoryId={
              route.categoryId
            }
            initialProductTypeId={
              route.productTypeId
            }

------------------------------------------------------------
行号：607
命中：initialCategoryId={

上文：
      return (
        <Suspense
          fallback={
            <ProductPageSkeleton
              variant="selection"
            />
          }
        >
          <ProductSelectionClient
            locale="zh"

>>>         initialCategoryId={

下文：
              route.categoryId
            }
            initialProductTypeId={
              route.productTypeId
            }
            initialFilters={
              route.initialFilters
            }
          />
        </Suspense>
      );
    }

------------------------------------------------------------
行号：608
命中：route.categoryId

上文：
        <Suspense
          fallback={
            <ProductPageSkeleton
              variant="selection"
            />
          }
        >
          <ProductSelectionClient
            locale="zh"
            initialCategoryId={

>>>           route.categoryId

下文：
            }
            initialProductTypeId={
              route.productTypeId
            }
            initialFilters={
              route.initialFilters
            }
          />
        </Suspense>
      );
    }

============================================================
文件：app\[locale]\products\[...segments]\page.tsx
============================================================

------------------------------------------------------------
行号：2
命中：import { notFound } from "next/navigation";

上文：
    import type { Metadata } from "next";

>>> import { notFound } from "next/navigation";

下文：
    import { Suspense } from "react";
    
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    import { englishProductDetailRoutes } from "@/data/products/product-detail-routes.generated";
    import {
      resolveCategoryRoute,
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";

------------------------------------------------------------
行号：9
命中：resolveCategoryRoute,

上文：
    import type { Metadata } from "next";
    import { notFound } from "next/navigation";
    import { Suspense } from "react";
    
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    import { englishProductDetailRoutes } from "@/data/products/product-detail-routes.generated";
    import {

>>>   resolveCategoryRoute,

下文：
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";

------------------------------------------------------------
行号：15
命中：import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";

上文：
    import ProductPageSkeleton from "@/components/common/ProductPageSkeleton";
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    import { englishProductDetailRoutes } from "@/data/products/product-detail-routes.generated";
    import {
      resolveCategoryRoute,
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    

>>> import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";

下文：
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    

------------------------------------------------------------
行号：16
命中：import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";

上文：
    import ProductSelectionClient from "@/components/products/selection/ProductSelectionClient";
    import { englishProductDetailRoutes } from "@/data/products/product-detail-routes.generated";
    import {
      resolveCategoryRoute,
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";

>>> import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";

下文：
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";

------------------------------------------------------------
行号：17
命中：import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";

上文：
    import { englishProductDetailRoutes } from "@/data/products/product-detail-routes.generated";
    import {
      resolveCategoryRoute,
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";

>>> import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";

下文：
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    

------------------------------------------------------------
行号：18
命中：import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";

上文：
    import {
      resolveCategoryRoute,
      resolveProductTypeRoute,
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";

>>> import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";

下文：
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {

------------------------------------------------------------
行号：21
命中：import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";

上文：
      resolveSeriesRoute,
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";

>>> import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";

下文：
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {
      params: Promise<{
        locale: string;
        segments: string[];

------------------------------------------------------------
行号：22
命中：import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";

上文：
    } from "@/data/products/selection/product-route-map";
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";

>>> import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";

下文：
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {
      params: Promise<{
        locale: string;
        segments: string[];
      }>;

------------------------------------------------------------
行号：23
命中：import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";

上文：
    import type { ProductRouteInitialFilters } from "@/data/products/selection/product-route-map";
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";

>>> import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";

下文：
    import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {
      params: Promise<{
        locale: string;
        segments: string[];
      }>;
    };

------------------------------------------------------------
行号：24
命中：import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";

上文：
    
    import ProductDetailRoutePage from "@/app/products/[category]/[slug]/page";
    import ProductsSeriesRoutePage from "@/app/products/[category]/[slug]/[seriesSlug]/page";
    import BarbedFittingDetailPage from "@/app/products/fittings/barbed-fittings/[slug]/page";
    import BulkheadBarbedFittingDetailPage from "@/app/products/fittings/bulkhead-barbed-fittings/[slug]/page";
    import CheckValveDetailPage from "@/app/products/fittings/check-valves/[slug]/page";
    import FilterDetailPage from "@/app/products/fittings/filters/[slug]/page";
    import QuickConnectFittingDetailPage from "@/app/products/fittings/quick-connect-fittings/[slug]/page";
    import QuickConnectQ20Page from "@/app/products/fittings/quick-connect-fittings/q20/page";
    import QuickConnectQ40Page from "@/app/products/fittings/quick-connect-fittings/q40/page";

>>> import QuickConnectQ60Page from "@/app/products/fittings/quick-connect-fittings/q60/page";

下文：
    import DiaphragmPumpDetailPage from "@/app/products/pumps/diaphragm-pumps/[slug]/page";
    import PipettingPumpDetailPage from "@/app/products/pumps/pipetting-pumps/[slug]/page";
    import PlungerPumpDetailPage from "@/app/products/pumps/plunger-pumps/[slug]/page";
    import SyringePumpDetailPage from "@/app/products/pumps/syringe-pumps/[slug]/page";
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {
      params: Promise<{
        locale: string;
        segments: string[];
      }>;
    };
    

------------------------------------------------------------
行号：39
命中：segments: string[];

上文：
    import ValvelessPumpDetailPage from "@/app/products/pumps/valveless-pumps/[slug]/page";
    import ProbeDetailPage from "@/app/products/probes/[slug]/page";
    import TubingDetailStaticPage from "@/app/products/tubing/_components/TubingDetailStaticPage";
    import ValveDetailPage from "@/app/products/valves/[slug]/page";
    
    import "@/app/products/products.css";
    
    type ProductLocaleRoutePageProps = {
      params: Promise<{
        locale: string;

>>>     segments: string[];

下文：
      }>;
    };
    
    const ROUTE_TITLES: Record<string, string> = {
      control: "Control Modules",
      fittings: "Fittings",
      probes: "Probes and Needles",
      pumps: "Pumps",
      tubing: "Tubing",
      valves: "Valves",
      "barbed-fittings": "Barbed Fittings",
      "bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",
      "luer-fittings": "Luer Fittings",

------------------------------------------------------------
行号：50
命中："barbed-fittings": "Barbed Fittings",

上文：
      }>;
    };
    
    const ROUTE_TITLES: Record<string, string> = {
      control: "Control Modules",
      fittings: "Fittings",
      probes: "Probes and Needles",
      pumps: "Pumps",
      tubing: "Tubing",
      valves: "Valves",

>>>   "barbed-fittings": "Barbed Fittings",

下文：
      "bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",
      "luer-fittings": "Luer Fittings",
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {

------------------------------------------------------------
行号：51
命中："bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",

上文：
    };
    
    const ROUTE_TITLES: Record<string, string> = {
      control: "Control Modules",
      fittings: "Fittings",
      probes: "Probes and Needles",
      pumps: "Pumps",
      tubing: "Tubing",
      valves: "Valves",
      "barbed-fittings": "Barbed Fittings",

>>>   "bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",

下文：
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",
      "luer-fittings": "Luer Fittings",
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {
      return englishProductDetailRoutes.map((segments) => ({

------------------------------------------------------------
行号：57
命中："luer-fittings": "Luer Fittings",

上文：
      pumps: "Pumps",
      tubing: "Tubing",
      valves: "Valves",
      "barbed-fittings": "Barbed Fittings",
      "bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",

>>>   "luer-fittings": "Luer Fittings",

下文：
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {
      return englishProductDetailRoutes.map((segments) => ({
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {

------------------------------------------------------------
行号：60
命中："quick-connect-fittings": "Quick-Connect Fittings",

上文：
      "barbed-fittings": "Barbed Fittings",
      "bulkhead-barbed-fittings": "Bulkhead Barbed Fittings",
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",
      "luer-fittings": "Luer Fittings",
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",

>>>   "quick-connect-fittings": "Quick-Connect Fittings",

下文：
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {
      return englishProductDetailRoutes.map((segments) => ({
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
        .filter(Boolean)

------------------------------------------------------------
行号：62
命中："thread-to-barbed-fittings": "Thread-to-Barb Fittings",

上文：
      "check-valves": "Check Valves",
      "diaphragm-pumps": "Diaphragm Pumps",
      "female-thread-adapters": "Female Thread Adapters",
      filters: "Inline Filters",
      "hard-tube-fittings": "Hard Tube Fittings",
      "luer-fittings": "Luer Fittings",
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",

>>>   "thread-to-barbed-fittings": "Thread-to-Barb Fittings",

下文：
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {
      return englishProductDetailRoutes.map((segments) => ({
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
        .filter(Boolean)
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {

------------------------------------------------------------
行号：68
命中：export function generateStaticParams() {

上文：
      "pipetting-pumps": "Pipetting Pumps",
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    

>>> export function generateStaticParams() {

下文：
      return englishProductDetailRoutes.map((segments) => ({
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
        .filter(Boolean)
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");

------------------------------------------------------------
行号：69
命中：return englishProductDetailRoutes.map((segments) => ({

上文：
      "plunger-pumps": "Plunger Pumps",
      "quick-connect-fittings": "Quick-Connect Fittings",
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {

>>>   return englishProductDetailRoutes.map((segments) => ({

下文：
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
        .filter(Boolean)
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");
    }

------------------------------------------------------------
行号：71
命中：segments: [...segments],

上文：
      "syringe-pumps": "Syringe Pumps",
      "thread-to-barbed-fittings": "Thread-to-Barb Fittings",
      "valveless-pumps": "Valveless Piston Pumps",
    };
    
    export const dynamicParams = false;
    
    export function generateStaticParams() {
      return englishProductDetailRoutes.map((segments) => ({
        locale: "en",

>>>     segments: [...segments],

下文：
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
        .filter(Boolean)
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");
    }
    
    function getRouteTitle(segments: string[]) {

------------------------------------------------------------
行号：89
命中：function getRouteTitle(segments: string[]) {

上文：
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");
    }
    

>>> function getRouteTitle(segments: string[]) {

下文：
      const lastSegment = segments.at(-1) ?? "products";
    
      return ROUTE_TITLES[lastSegment] || titleFromSlug(lastSegment);
    }
    
    function routeExists(segments: string[]) {
      const routeKey = segments.join("/");
    
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({
      categoryId,
      productTypeId,
      initialFilters,
    }: {

------------------------------------------------------------
行号：90
命中：const lastSegment = segments.at(-1) ?? "products";

上文：
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");
    }
    
    function getRouteTitle(segments: string[]) {

>>>   const lastSegment = segments.at(-1) ?? "products";

下文：
    
      return ROUTE_TITLES[lastSegment] || titleFromSlug(lastSegment);
    }
    
    function routeExists(segments: string[]) {
      const routeKey = segments.join("/");
    
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({
      categoryId,
      productTypeId,
      initialFilters,
    }: {
      categoryId: string;

------------------------------------------------------------
行号：95
命中：function routeExists(segments: string[]) {

上文：
        })
        .join(" ");
    }
    
    function getRouteTitle(segments: string[]) {
      const lastSegment = segments.at(-1) ?? "products";
    
      return ROUTE_TITLES[lastSegment] || titleFromSlug(lastSegment);
    }
    

>>> function routeExists(segments: string[]) {

下文：
      const routeKey = segments.join("/");
    
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({
      categoryId,
      productTypeId,
      initialFilters,
    }: {
      categoryId: string;
      productTypeId?: string;
      initialFilters?: ProductRouteInitialFilters;
    }) {
      return (
        <Suspense fallback={<ProductPageSkeleton variant="selection" />}>

------------------------------------------------------------
行号：96
命中：const routeKey = segments.join("/");

上文：
        .join(" ");
    }
    
    function getRouteTitle(segments: string[]) {
      const lastSegment = segments.at(-1) ?? "products";
    
      return ROUTE_TITLES[lastSegment] || titleFromSlug(lastSegment);
    }
    
    function routeExists(segments: string[]) {

>>>   const routeKey = segments.join("/");

下文：
    
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({
      categoryId,
      productTypeId,
      initialFilters,
    }: {
      categoryId: string;
      productTypeId?: string;
      initialFilters?: ProductRouteInitialFilters;
    }) {
      return (
        <Suspense fallback={<ProductPageSkeleton variant="selection" />}>
          <ProductSelectionClient

------------------------------------------------------------
行号：104
命中：categoryId,

上文：
    
    function routeExists(segments: string[]) {
      const routeKey = segments.join("/");
    
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({

>>>   categoryId,

下文：
      productTypeId,
      initialFilters,
    }: {
      categoryId: string;
      productTypeId?: string;
      initialFilters?: ProductRouteInitialFilters;
    }) {
      return (
        <Suspense fallback={<ProductPageSkeleton variant="selection" />}>
          <ProductSelectionClient
            locale="en"
            initialCategoryId={categoryId}
            initialProductTypeId={productTypeId}
            initialFilters={initialFilters}
          />
        </Suspense>
      );
    }

------------------------------------------------------------
行号：108
命中：categoryId: string;

上文：
      return englishProductDetailRoutes.some(
        (route) => route.join("/") === routeKey
      );
    }
    
    function renderSelectionPage({
      categoryId,
      productTypeId,
      initialFilters,
    }: {

>>>   categoryId: string;

下文：
      productTypeId?: string;
      initialFilters?: ProductRouteInitialFilters;
    }) {
      return (
        <Suspense fallback={<ProductPageSkeleton variant="selection" />}>
          <ProductSelectionClient
            locale="en"
            initialCategoryId={categoryId}
            initialProductTypeId={productTypeId}
            initialFilters={initialFilters}
          />
        </Suspense>
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {

------------------------------------------------------------
行号：116
命中：initialCategoryId={categoryId}

上文：
      initialFilters,
    }: {
      categoryId: string;
      productTypeId?: string;
      initialFilters?: ProductRouteInitialFilters;
    }) {
      return (
        <Suspense fallback={<ProductPageSkeleton variant="selection" />}>
          <ProductSelectionClient
            locale="en"

>>>         initialCategoryId={categoryId}

下文：
            initialProductTypeId={productTypeId}
            initialFilters={initialFilters}
          />
        </Suspense>
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        return {};
      }
    
      const title = getRouteTitle(segments);
      const canonicalPath = `/en/products/${segments.join("/")}/`;

------------------------------------------------------------
行号：127
命中：const { locale, segments } = await params;

上文：
            initialProductTypeId={productTypeId}
            initialFilters={initialFilters}
          />
        </Suspense>
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {

>>>   const { locale, segments } = await params;

下文：
    
      if (locale !== "en" || !routeExists(segments)) {
        return {};
      }
    
      const title = getRouteTitle(segments);
      const canonicalPath = `/en/products/${segments.join("/")}/`;
      const isDetailRoute = segments.length >= 2;
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
        new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",

------------------------------------------------------------
行号：129
命中：if (locale !== "en" || !routeExists(segments)) {

上文：
          />
        </Suspense>
      );
    }
    
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {
      const { locale, segments } = await params;
    

>>>   if (locale !== "en" || !routeExists(segments)) {

下文：
        return {};
      }
    
      const title = getRouteTitle(segments);
      const canonicalPath = `/en/products/${segments.join("/")}/`;
      const isDetailRoute = segments.length >= 2;
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
        new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",
          ...(isDetailRoute ? ["product specifications", "model configurations"] : []),
        ])

------------------------------------------------------------
行号：133
命中：const title = getRouteTitle(segments);

上文：
    
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        return {};
      }
    

>>>   const title = getRouteTitle(segments);

下文：
      const canonicalPath = `/en/products/${segments.join("/")}/`;
      const isDetailRoute = segments.length >= 2;
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
        new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",
          ...(isDetailRoute ? ["product specifications", "model configurations"] : []),
        ])
      );
    
      return {
        title: `${title} | FOREACH`,

------------------------------------------------------------
行号：134
命中：const canonicalPath = `/en/products/${segments.join("/")}/`;

上文：
    export async function generateMetadata({
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        return {};
      }
    
      const title = getRouteTitle(segments);

>>>   const canonicalPath = `/en/products/${segments.join("/")}/`;

下文：
      const isDetailRoute = segments.length >= 2;
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
        new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",
          ...(isDetailRoute ? ["product specifications", "model configurations"] : []),
        ])
      );
    
      return {
        title: `${title} | FOREACH`,
        description,

------------------------------------------------------------
行号：135
命中：const isDetailRoute = segments.length >= 2;

上文：
      params,
    }: ProductLocaleRoutePageProps): Promise<Metadata> {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        return {};
      }
    
      const title = getRouteTitle(segments);
      const canonicalPath = `/en/products/${segments.join("/")}/`;

>>>   const isDetailRoute = segments.length >= 2;

下文：
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
        new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",
          ...(isDetailRoute ? ["product specifications", "model configurations"] : []),
        ])
      );
    
      return {
        title: `${title} | FOREACH`,
        description,
        keywords,

------------------------------------------------------------
行号：183
命中：const { locale, segments } = await params;

上文：
          card: "summary",
          title: `${title} | FOREACH`,
          description,
        },
      };
    }
    
    export default async function ProductLocaleRoutePage({
      params,
    }: ProductLocaleRoutePageProps) {

>>>   const { locale, segments } = await params;

下文：
    
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }

------------------------------------------------------------
行号：185
命中：if (locale !== "en" || !routeExists(segments)) {

上文：
          description,
        },
      };
    }
    
    export default async function ProductLocaleRoutePage({
      params,
    }: ProductLocaleRoutePageProps) {
      const { locale, segments } = await params;
    

>>>   if (locale !== "en" || !routeExists(segments)) {

下文：
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {

------------------------------------------------------------
行号：186
命中：notFound();

上文：
        },
      };
    }
    
    export default async function ProductLocaleRoutePage({
      params,
    }: ProductLocaleRoutePageProps) {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {

>>>     notFound();

下文：
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);

------------------------------------------------------------
行号：189
命中：const [category, slug, seriesSlug] = segments;

上文：
    
    export default async function ProductLocaleRoutePage({
      params,
    }: ProductLocaleRoutePageProps) {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    

>>>   const [category, slug, seriesSlug] = segments;

下文：
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({

------------------------------------------------------------
行号：191
命中：if (segments.length === 1) {

上文：
      params,
    }: ProductLocaleRoutePageProps) {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    

>>>   if (segments.length === 1) {

下文：
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,

------------------------------------------------------------
行号：192
命中：const categoryRoute = resolveCategoryRoute(category);

上文：
    }: ProductLocaleRoutePageProps) {
      const { locale, segments } = await params;
    
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {

>>>     const categoryRoute = resolveCategoryRoute(category);

下文：
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });

------------------------------------------------------------
行号：194
命中：if (!categoryRoute) {

上文：
    
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    

>>>     if (!categoryRoute) {

下文：
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    

------------------------------------------------------------
行号：195
命中：notFound();

上文：
      if (locale !== "en" || !routeExists(segments)) {
        notFound();
      }
    
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {

>>>       notFound();

下文：
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {

------------------------------------------------------------
行号：199
命中：categoryId: categoryRoute.categoryId,

上文：
      const [category, slug, seriesSlug] = segments;
    
      if (segments.length === 1) {
        const categoryRoute = resolveCategoryRoute(category);
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({

>>>       categoryId: categoryRoute.categoryId,

下文：
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {

------------------------------------------------------------
行号：203
命中：if (segments.length === 2) {

上文：
    
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    

>>>   if (segments.length === 2) {

下文：
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {

------------------------------------------------------------
行号：204
命中：const productTypeRoute = resolveProductTypeRoute(category, slug);

上文：
        if (!categoryRoute) {
          notFound();
        }
    
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {

>>>     const productTypeRoute = resolveProductTypeRoute(category, slug);

下文：
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;

------------------------------------------------------------
行号：208
命中：categoryId: productTypeRoute.categoryId,

上文：
        return renderSelectionPage({
          categoryId: categoryRoute.categoryId,
        });
      }
    
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({

>>>         categoryId: productTypeRoute.categoryId,

下文：
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),

------------------------------------------------------------
行号：213
命中：if (category === "valves") {

上文：
      if (segments.length === 2) {
        const productTypeRoute = resolveProductTypeRoute(category, slug);
    
        if (productTypeRoute) {
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    

>>>     if (category === "valves") {

下文：
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {
        notFound();

------------------------------------------------------------
行号：217
命中：if (category === "probes") {

上文：
          return renderSelectionPage({
            categoryId: productTypeRoute.categoryId,
            productTypeId: productTypeRoute.productTypeId,
          });
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    

>>>     if (category === "probes") {

下文：
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (
        category === "fittings" &&

------------------------------------------------------------
行号：221
命中：if (category === "tubing") {

上文：
        }
    
        if (category === "valves") {
          return ValveDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    

>>>     if (category === "tubing") {

下文：
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;

------------------------------------------------------------
行号：226
命中：params: Promise.resolve({ category, slug }),

上文：
    
        if (category === "probes") {
          return ProbeDetailPage({ params: Promise.resolve({ slug }) });
        }
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({

>>>       params: Promise.resolve({ category, slug }),

下文：
        });
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }

------------------------------------------------------------
行号：230
命中：if (segments.length !== 3) {

上文：
    
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    

>>>   if (segments.length !== 3) {

下文：
        notFound();
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }

------------------------------------------------------------
行号：231
命中：notFound();

上文：
        if (category === "tubing") {
          return <TubingDetailStaticPage slug={slug} />;
        }
    
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {

>>>     notFound();

下文：
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }

------------------------------------------------------------
行号：235
命中：category === "fittings" &&

上文：
        return ProductDetailRoutePage({
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (

>>>     category === "fittings" &&

下文：
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {

------------------------------------------------------------
行号：236
命中：slug === "quick-connect-fittings"

上文：
          params: Promise.resolve({ category, slug }),
        });
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (
        category === "fittings" &&

>>>     slug === "quick-connect-fittings"

下文：
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({

------------------------------------------------------------
行号：238
命中：if (seriesSlug === "q20") {

上文：
      }
    
      if (segments.length !== 3) {
        notFound();
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {

>>>     if (seriesSlug === "q20") {

下文：
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,

------------------------------------------------------------
行号：242
命中：if (seriesSlug === "q40") {

上文：
      }
    
      if (
        category === "fittings" &&
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    

>>>     if (seriesSlug === "q40") {

下文：
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    

------------------------------------------------------------
行号：246
命中：if (seriesSlug === "q60") {

上文：
        slug === "quick-connect-fittings"
      ) {
        if (seriesSlug === "q20") {
          return <QuickConnectQ20Page />;
        }
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    

>>>     if (seriesSlug === "q60") {

下文：
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    
      if (category === "pumps") {
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "plunger-pumps") {

------------------------------------------------------------
行号：251
命中：const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);

上文：
    
        if (seriesSlug === "q40") {
          return <QuickConnectQ40Page />;
        }
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    

>>>   const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);

下文：
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    
      if (category === "pumps") {
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "plunger-pumps") {
          return PlungerPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "diaphragm-pumps") {
          return DiaphragmPumpDetailPage({ params: detailParams });

------------------------------------------------------------
行号：255
命中：categoryId: seriesRoute.categoryId,

上文：
    
        if (seriesSlug === "q60") {
          return <QuickConnectQ60Page />;
        }
      }
    
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({

>>>       categoryId: seriesRoute.categoryId,

下文：
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    
      if (category === "pumps") {
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "plunger-pumps") {
          return PlungerPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "diaphragm-pumps") {
          return DiaphragmPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "pipetting-pumps") {
          return PipettingPumpDetailPage({ params: detailParams });

------------------------------------------------------------
行号：261
命中：if (category === "pumps") {

上文：
      const seriesRoute = resolveSeriesRoute(category, slug, seriesSlug);
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    

>>>   if (category === "pumps") {

下文：
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "plunger-pumps") {
          return PlungerPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "diaphragm-pumps") {
          return DiaphragmPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "pipetting-pumps") {
          return PipettingPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "syringe-pumps") {
          return SyringePumpDetailPage({ params: detailParams });
        }
    

------------------------------------------------------------
行号：262
命中：const detailParams = Promise.resolve({ slug: seriesSlug });

上文：
    
      if (seriesRoute) {
        return renderSelectionPage({
          categoryId: seriesRoute.categoryId,
          productTypeId: seriesRoute.productTypeId,
          initialFilters: seriesRoute.initialFilters,
        });
      }
    
      if (category === "pumps") {

>>>     const detailParams = Promise.resolve({ slug: seriesSlug });

下文：
    
        if (slug === "plunger-pumps") {
          return PlungerPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "diaphragm-pumps") {
          return DiaphragmPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "pipetting-pumps") {
          return PipettingPumpDetailPage({ params: detailParams });
        }
    
        if (slug === "syringe-pumps") {
          return SyringePumpDetailPage({ params: detailParams });
        }
    
        if (slug === "valveless-pumps") {

------------------------------------------------------------
行号：285
命中：if (category === "fittings") {

上文：
    
        if (slug === "syringe-pumps") {
          return SyringePumpDetailPage({ params: detailParams });
        }
    
        if (slug === "valveless-pumps") {
          return ValvelessPumpDetailPage({ params: detailParams });
        }
      }
    

>>>   if (category === "fittings") {

下文：
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "barbed-fittings") {
          return BarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "quick-connect-fittings") {
          return QuickConnectFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "bulkhead-barbed-fittings") {
          return BulkheadBarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    

------------------------------------------------------------
行号：286
命中：const detailParams = Promise.resolve({ slug: seriesSlug });

上文：
        if (slug === "syringe-pumps") {
          return SyringePumpDetailPage({ params: detailParams });
        }
    
        if (slug === "valveless-pumps") {
          return ValvelessPumpDetailPage({ params: detailParams });
        }
      }
    
      if (category === "fittings") {

>>>     const detailParams = Promise.resolve({ slug: seriesSlug });

下文：
    
        if (slug === "barbed-fittings") {
          return BarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "quick-connect-fittings") {
          return QuickConnectFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "bulkhead-barbed-fittings") {
          return BulkheadBarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    
        if (slug === "check-valves") {

------------------------------------------------------------
行号：288
命中：if (slug === "barbed-fittings") {

上文：
        }
    
        if (slug === "valveless-pumps") {
          return ValvelessPumpDetailPage({ params: detailParams });
        }
      }
    
      if (category === "fittings") {
        const detailParams = Promise.resolve({ slug: seriesSlug });
    

>>>     if (slug === "barbed-fittings") {

下文：
          return BarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "quick-connect-fittings") {
          return QuickConnectFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "bulkhead-barbed-fittings") {
          return BulkheadBarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    
        if (slug === "check-valves") {
          return CheckValveDetailPage({ params: detailParams });
        }

------------------------------------------------------------
行号：292
命中：if (slug === "quick-connect-fittings") {

上文：
        }
      }
    
      if (category === "fittings") {
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "barbed-fittings") {
          return BarbedFittingDetailPage({ params: detailParams });
        }
    

>>>     if (slug === "quick-connect-fittings") {

下文：
          return QuickConnectFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "bulkhead-barbed-fittings") {
          return BulkheadBarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    
        if (slug === "check-valves") {
          return CheckValveDetailPage({ params: detailParams });
        }
      }
    
      return ProductsSeriesRoutePage({
        params: Promise.resolve({ category, slug, seriesSlug }),

------------------------------------------------------------
行号：296
命中：if (slug === "bulkhead-barbed-fittings") {

上文：
        const detailParams = Promise.resolve({ slug: seriesSlug });
    
        if (slug === "barbed-fittings") {
          return BarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "quick-connect-fittings") {
          return QuickConnectFittingDetailPage({ params: detailParams });
        }
    

>>>     if (slug === "bulkhead-barbed-fittings") {

下文：
          return BulkheadBarbedFittingDetailPage({ params: detailParams });
        }
    
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    
        if (slug === "check-valves") {
          return CheckValveDetailPage({ params: detailParams });
        }
      }
    
      return ProductsSeriesRoutePage({
        params: Promise.resolve({ category, slug, seriesSlug }),
      });
    }

------------------------------------------------------------
行号：310
命中：params: Promise.resolve({ category, slug, seriesSlug }),

上文：
        if (slug === "filters") {
          return FilterDetailPage({ params: detailParams });
        }
    
        if (slug === "check-valves") {
          return CheckValveDetailPage({ params: detailParams });
        }
      }
    
      return ProductsSeriesRoutePage({

>>>     params: Promise.resolve({ category, slug, seriesSlug }),

下文：
      });
    }

============================================================
文件：app\products\fittings\barbed-fittings\[slug]\page.tsx
============================================================

------------------------------------------------------------
行号：10
命中：notFound,

上文：
    import type {
      ComponentType,
    } from "react";
    
    import type {
      Metadata,
    } from "next";
    
    import {

>>>   notFound,

下文：
    } from "next/navigation";
    
    import ProductDetailClient from "@/components/products/detail/ProductDetailClient";
    import detailsJson from "@/data/products/generated/fittings/barbed-fittings/detail/index.json";
    
    import "../../../products.css";
    
    type DetailRecord = {
      slug: string;
      model: string;
      name?: string;
      title?: string;
      description?: string;
      seo?: {
        title?: string;
        description?: string;
      };
      [key: string]: unknown;

------------------------------------------------------------
行号：13
命中：import ProductDetailClient from "@/components/products/detail/ProductDetailClient";

上文：
    } from "react";
    
    import type {
      Metadata,
    } from "next";
    
    import {
      notFound,
    } from "next/navigation";
    

>>> import ProductDetailClient from "@/components/products/detail/ProductDetailClient";

下文：
    import detailsJson from "@/data/products/generated/fittings/barbed-fittings/detail/index.json";
    
    import "../../../products.css";
    
    type DetailRecord = {
      slug: string;
      model: string;
      name?: string;
      title?: string;
      description?: string;
      seo?: {
        title?: string;
        description?: string;
      };
      [key: string]: unknown;
    };
    
    type PageProps = {

------------------------------------------------------------
行号：14
命中：import detailsJson from "@/data/products/generated/fittings/barbed-fittings/detail/index.json";

上文：
    
    import type {
      Metadata,
    } from "next";
    
    import {
      notFound,
    } from "next/navigation";
    
    import ProductDetailClient from "@/components/products/detail/ProductDetailClient";

>>> import detailsJson from "@/data/products/generated/fittings/barbed-fittings/detail/index.json";

下文：
    
    import "../../../products.css";
    
    type DetailRecord = {
      slug: string;
      model: string;
      name?: string;
      title?: string;
      description?: string;
      seo?: {
        title?: string;
        description?: string;
      };
      [key: string]: unknown;
    };
    
    type PageProps = {
      params: Promise<{

------------------------------------------------------------
行号：38
命中：detailsJson as DetailRecord[];

上文：
      [key: string]: unknown;
    };
    
    type PageProps = {
      params: Promise<{
        slug: string;
      }>;
    };
    
    const details =

>>>   detailsJson as DetailRecord[];

下文：
    
    const ProductDetailView =
      ProductDetailClient as unknown as ComponentType<{
        data: any;
      }>;
    
    export const dynamicParams =
      false;
    
    function normalizeSegment(
      value: string
    ) {
      return String(value || "")
        .trim()
        .toLowerCase();
    }
    
    function findDetail(

------------------------------------------------------------
行号：41
命中：ProductDetailClient as unknown as ComponentType<{

上文：
    type PageProps = {
      params: Promise<{
        slug: string;
      }>;
    };
    
    const details =
      detailsJson as DetailRecord[];
    
    const ProductDetailView =

>>>   ProductDetailClient as unknown as ComponentType<{

下文：
        data: any;
      }>;
    
    export const dynamicParams =
      false;
    
    function normalizeSegment(
      value: string
    ) {
      return String(value || "")
        .trim()
        .toLowerCase();
    }
    
    function findDetail(
      slug: string
    ) {
      const target =

------------------------------------------------------------
行号：70
命中：export function generateStaticParams() {

上文：
        normalizeSegment(slug);
    
      return details.find(
        (item) =>
          normalizeSegment(
            item.slug
          ) === target
      );
    }
    

>>> export function generateStaticParams() {

下文：
      return details.map(
        (detail) => ({
          slug:
            detail.slug,
        })
      );
    }
    
    export async function generateMetadata({
      params,
    }: PageProps): Promise<Metadata> {
      const {
        slug,
      } = await params;
    
      const detail =
        findDetail(slug);
    

------------------------------------------------------------
行号：115
命中：notFound();

上文：
      params,
    }: PageProps) {
      const {
        slug,
      } = await params;
    
      const detail =
        findDetail(slug);
    
      if (!detail) {

>>>     notFound();

下文：
      }
    
      return (
        <ProductDetailView
          data={detail}
        />
      );
    }

============================================================
文件：data\products\generated\fittings\luer-fittings\detail\index.json
============================================================

------------------------------------------------------------
行号：15
命中："slug": "lsl-16-pp-n",

上文：
        "productId": "809558",
        "productCode": "809558",
        "seriesId": "lsl",
        "seriesCode": "LSL",
        "seriesName": "固定公鲁尔芯子",

>>>     "slug": "lsl-16-pp-n",

下文：
        "model": "LSL-16-PP-N",
        "name": "固定公鲁尔芯子",
        "title": "固定公鲁尔芯子",
        "displayName": "固定公鲁尔芯子",
        "productName": "固定公鲁尔芯子",
        "modelDisplay": "LSL-16-PP-N",
        "displayModel": "LSL-16-PP-N",
        "foreachModel": "LSL-16-PP-N",

------------------------------------------------------------
行号：16
命中："model": "LSL-16-PP-N",

上文：
        "productCode": "809558",
        "seriesId": "lsl",
        "seriesCode": "LSL",
        "seriesName": "固定公鲁尔芯子",
        "slug": "lsl-16-pp-n",

>>>     "model": "LSL-16-PP-N",

下文：
        "name": "固定公鲁尔芯子",
        "title": "固定公鲁尔芯子",
        "displayName": "固定公鲁尔芯子",
        "productName": "固定公鲁尔芯子",
        "modelDisplay": "LSL-16-PP-N",
        "displayModel": "LSL-16-PP-N",
        "foreachModel": "LSL-16-PP-N",
        "description": "LSL-16-PP-N是一款固定公鲁尔芯子，适配1.6 mm内径软管，主体材质为PP，适用于微流体系统、IVD仪器、分析仪器及实验室自动化设备中的液路连接。",

------------------------------------------------------------
行号：145
命中："detailHref": "/products/fittings/luer-fittings/lsl-16-pp-n",

上文：
            "answer": "标准颜色以在售型号为准，其他颜色需求可在提交清单时说明。"
          }
        ],
        "showDrawingRequest": true,
        "show3DRequest": false,

>>>     "detailHref": "/products/fittings/luer-fittings/lsl-16-pp-n",

下文：
        "selectionHref": "/products/fittings/luer-fittings",
        "configuratorHref": "/products/fittings/luer-fittings",
        "modelSelectionHref": "/products/fittings/luer-fittings",
        "resourceStatus": {
          "model3d": "missing",
          "drawing": "available"
        },
        "resourceMessages": {

============================================================
需要确认的结果
============================================================

1. 中文三层动态路由是否把 seriesSlug 当作产品详情 slug。
2. 英文 catch-all 路由是否识别 fittings/luer-fittings/[modelSlug]。
3. 两个通用路由是否读取鲁尔 detail/index.json。
4. 如果都没有读取，则应新建鲁尔接头专属详情页面。

