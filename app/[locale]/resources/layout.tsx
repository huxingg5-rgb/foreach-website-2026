import type { ReactNode } from "react";

import "../../resources/resource-banner.css";

export default function LocalizedResourcesLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
