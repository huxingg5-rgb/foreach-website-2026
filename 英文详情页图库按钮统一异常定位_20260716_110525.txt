英文详情页图库按钮统一异常定位

检查时间：2026-07-16 11:05:25
项目目录：F:\WebsiteProjects\foreach-website-2026
本次只检查，没有修改文件，也没有运行 npm build。

初步判断：英文详情适配层可能把同一路径的多个图片字段重复加入图库。

============================================================
文件：app\[locale]\products\[...segments]\page.tsx
============================================================

------------------------------------------------------------
行号：67
命中：.filter(Boolean)

        luerDetailsJson as
          LuerEnglishDetailRoute[]
      )
        .map((detail) =>
          String(
            detail.slug || ""
          )
            .trim()
            .toLowerCase()
        )
>>>     .filter(Boolean)
        .map((detailSlug) => [
          "fittings",
          "luer-fittings",
          detailSlug,
        ]);
    
    const allEnglishProductDetailRoutes:
      string[][] = [
        ...englishProductDetailRoutes.map(
          (segments) => [
            ...segments,
          ]
        ),
        ...luerEnglishProductDetailRoutes,
      ];
    
    /* LUER_ENGLISH_DETAIL_ROUTES_END */
    

------------------------------------------------------------
行号：121
命中：.filter(Boolean)

    export function generateStaticParams() {
      return allEnglishProductDetailRoutes.map((segments) => ({
        locale: "en",
        segments: [...segments],
      }));
    }
    
    function titleFromSlug(slug: string) {
      return slug
        .split("-")
>>>     .filter(Boolean)
        .map((part) => {
          if (/\d/.test(part) || part.length <= 4) {
            return part.toUpperCase();
          }
    
          return `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`;
        })
        .join(" ");
    }
    
    function getRouteTitle(segments: string[]) {
      const lastSegment = segments.at(-1) ?? "products";
    
      return ROUTE_TITLES[lastSegment] || titleFromSlug(lastSegment);
    }
    
    function routeExists(segments: string[]) {
      const routeKey = segments.join("/");

------------------------------------------------------------
行号：183
命中：new Set([

        return {};
      }
    
      const title = getRouteTitle(segments);
      const canonicalPath = `/en/products/${segments.join("/")}/`;
      const isDetailRoute = segments.length >= 2;
      const description = isDetailRoute
        ? `Explore ${title} specifications, materials, interfaces, model configurations, and fluidic applications from FOREACH.`
        : `Explore FOREACH ${title} for precision fluid handling in IVD, life science, analytical instrumentation, and laboratory automation.`;
      const keywords = Array.from(
>>>     new Set([
          title,
          "FOREACH",
          "precision fluid handling",
          "microfluidic components",
          "fluidic systems",
          ...(isDetailRoute ? ["product specifications", "model configurations"] : []),
        ])
      );
    
      return {
        title: `${title} | FOREACH`,
        description,
        keywords,
        alternates: {
          canonical: canonicalPath,
          languages: {
            "en-US": canonicalPath,
          },

============================================================
文件：components\products\detail\ProductDetailClient.tsx
============================================================

------------------------------------------------------------
行号：98
命中：return data?.__locale === "en"

        (typeof data?.slug === "string" && data.slug.includes("-tubing"))
      );
    }
    
    function getDisplayModelText(data: any): string {
      if (isTubingDetailData(data)) {
        return "XXX-XXX-XX-XX";
      }
    
      if (isCustomInquiryMode(data)) {
>>>     return data?.__locale === "en"
          ? "Contact us for a custom configuration"
          : "定制配置请联系我们";
      }
    
      return (data as any).displayModel || data.model || "";
    }
    
    function isCustomInquiryMode(data: any): boolean {
      
      if (
        isValvelessPumpDetailData(data) ||
        data?.isCustomOnly === true ||
        data?.showCustomInquiryCta === true
      ) {
        return true;
      }
    const detailMode = String(
        data?.detailMode ||

------------------------------------------------------------
行号：165
命中：return data?.__locale === "en" ? "Select a Model" : "选择型号";

        data?.sourceType === "fitting-detail" ||
        (
          data?.categoryId === "fittings" &&
          data?.productTypeId === "hard-tube-fittings"
        )
      );
    }
    
    function getModelActionText(data: any): string {
      if (isTubingDetailData(data)) {
>>>     return data?.__locale === "en" ? "Select a Model" : "选择型号";
      }
    
      if (data?.__locale === "en") {
        return isCustomInquiryMode(data) ? "Contact Us" : "Select a Model";
      }
    
      return isCustomInquiryMode(data) ? "联系我们" : "型号选择";
    }
    
    function isPlungerPumpDetailData(data: any): boolean {
      const text = JSON.stringify(data || {}).toLowerCase();
    
      return (
        text.includes("柱塞泵") ||
        text.includes("plunger pump") ||
        text.includes("plunger-pumps") ||
        text.includes("piston pump") ||
        text.includes("ea-") ||

------------------------------------------------------------
行号：168
命中：if (data?.__locale === "en") {

          data?.productTypeId === "hard-tube-fittings"
        )
      );
    }
    
    function getModelActionText(data: any): string {
      if (isTubingDetailData(data)) {
        return data?.__locale === "en" ? "Select a Model" : "选择型号";
      }
    
>>>   if (data?.__locale === "en") {
        return isCustomInquiryMode(data) ? "Contact Us" : "Select a Model";
      }
    
      return isCustomInquiryMode(data) ? "联系我们" : "型号选择";
    }
    
    function isPlungerPumpDetailData(data: any): boolean {
      const text = JSON.stringify(data || {}).toLowerCase();
    
      return (
        text.includes("柱塞泵") ||
        text.includes("plunger pump") ||
        text.includes("plunger-pumps") ||
        text.includes("piston pump") ||
        text.includes("ea-") ||
        text.includes("eas-") ||
        text.includes("sm-") ||
        text.includes("tm-")

------------------------------------------------------------
行号：430
命中：if (data?.__locale === "en") {

        return probeBottomCta;
      }
    
      const valveBottomCta = getValveDetailBottomCta(data);
    
      if (valveBottomCta) {
        return valveBottomCta;
      }
    
      if (isSyringePumpDetailData(data)) {
>>>     if (data?.__locale === "en") {
          return {
            title: "Syringe pumps configured for your fluidic system",
            desc: "Share the syringe size, stroke, channel count, valve arrangement, communication interface, installation space, and fluidic integration requirements. The FOREACH engineering team can help confirm a suitable configuration.",
            button: "Submit a Custom Request",
            href: "/en/contact",
          };
        }
    
        return {
          title: "注射泵可根据您的液路与结构需求进行定制",
          desc: "恒永达可根据您的应用场景、注射器规格、行程平台、通道数量、阀门结构、通讯方式、安装空间和液路集成需求，协助确认适合自动化仪器集成的注射泵配置。",
          button: "提交定制需求",
          href: "/contact",
        };
      }
    
      if (isValvelessPumpDetailData(data)) {
        if (data?.__locale === "en") {

------------------------------------------------------------
行号：448
命中：if (data?.__locale === "en") {

    
        return {
          title: "注射泵可根据您的液路与结构需求进行定制",
          desc: "恒永达可根据您的应用场景、注射器规格、行程平台、通道数量、阀门结构、通讯方式、安装空间和液路集成需求，协助确认适合自动化仪器集成的注射泵配置。",
          button: "提交定制需求",
          href: "/contact",
        };
      }
    
      if (isValvelessPumpDetailData(data)) {
>>>     if (data?.__locale === "en") {
          return {
            title: "Valveless pumps configured for your fluidic requirements",
            desc: "Share the target displacement, ratio requirements, fluid compatibility, port type, cleaning requirements, and installation space. The FOREACH engineering team can help confirm a suitable configuration.",
            button: "Submit a Custom Request",
            href: "/en/contact",
          };
        }
    
        return {
          title: "无阀泵可根据您的液路需求进行定制",
          desc: "恒永达可根据您的应用场景、目标排量、配比要求、液体兼容性、接口方式、清洗口和安装空间，协助确认适合自动化仪器集成的无阀泵配置。",
          button: "提交定制需求",
          href: "/contact",
        };
      }
      if (isPlungerPumpDetailData(data)) {
        if (data?.__locale === "en") {
          return {

------------------------------------------------------------
行号：465
命中：if (data?.__locale === "en") {

        }
    
        return {
          title: "无阀泵可根据您的液路需求进行定制",
          desc: "恒永达可根据您的应用场景、目标排量、配比要求、液体兼容性、接口方式、清洗口和安装空间，协助确认适合自动化仪器集成的无阀泵配置。",
          button: "提交定制需求",
          href: "/contact",
        };
      }
      if (isPlungerPumpDetailData(data)) {
>>>     if (data?.__locale === "en") {
          return {
            title: "Plunger pumps configured for your instrument",
            desc: "Share the target volume, fluid compatibility, port type, control method, installation space, and service-life requirements. The FOREACH engineering team can help confirm the pump configuration and wetted materials.",
            button: "Submit a Custom Request",
            href: "/en/contact",
          };
        }
    
        return {
          title: "柱塞泵可根据您的设备需求进行定制",
          desc: "恒永达可根据您的设备结构、目标容量、液体兼容性、接口方式、控制方式和使用寿命要求，协助确认柱塞泵配置、泵头材质、柱塞材质及液路集成方案，适用于 IVD 分析仪、实验室自动化设备和生命科学仪器中的精密液体处理场景。",
          button: "提交定制需求",
          href: "/contact"
        };
      }
    
      if (isDiaphragmPumpDetailData(data)) {
        if (data?.__locale === "en") {

------------------------------------------------------------
行号：483
命中：if (data?.__locale === "en") {

    
        return {
          title: "柱塞泵可根据您的设备需求进行定制",
          desc: "恒永达可根据您的设备结构、目标容量、液体兼容性、接口方式、控制方式和使用寿命要求，协助确认柱塞泵配置、泵头材质、柱塞材质及液路集成方案，适用于 IVD 分析仪、实验室自动化设备和生命科学仪器中的精密液体处理场景。",
          button: "提交定制需求",
          href: "/contact"
        };
      }
    
      if (isDiaphragmPumpDetailData(data)) {
>>>     if (data?.__locale === "en") {
          return {
            title: "Need help selecting a diaphragm pump?",
            desc: "Share the fluid, flow rate, pressure, self-priming requirements, wetted materials, port type, and installation space. The FOREACH engineering team can help confirm a suitable diaphragm pump configuration.",
            button: "Contact an Engineer",
            href: "/en/contact",
          };
        }
    
        return {
          title: "不确定如何选择隔膜泵型号？",
          desc: "如果您不确定具体型号，可根据介质类型、流量、耐压、自吸能力、膜片材质、阀片材质、泵头材质、接口方式和安装空间等信息联系我们。恒永达可协助您确认适合自动化仪器液路的隔膜泵配置。",
          button: "联系工程师确认",
          href: "/contact"
        };
      }
    
      if (isPipettingPumpDetailData(data)) {
        if (data?.__locale === "en") {

------------------------------------------------------------
行号：501
命中：if (data?.__locale === "en") {

    
        return {
          title: "不确定如何选择隔膜泵型号？",
          desc: "如果您不确定具体型号，可根据介质类型、流量、耐压、自吸能力、膜片材质、阀片材质、泵头材质、接口方式和安装空间等信息联系我们。恒永达可协助您确认适合自动化仪器液路的隔膜泵配置。",
          button: "联系工程师确认",
          href: "/contact"
        };
      }
    
      if (isPipettingPumpDetailData(data)) {
>>>     if (data?.__locale === "en") {
          return {
            title: "Need help selecting a pipetting pump?",
            desc: "Share the volume range, tip specification, liquid-level and clog-detection requirements, communication interface, installation space, and control method. The FOREACH engineering team can help confirm a suitable configuration.",
            button: "Contact an Engineer",
            href: "/en/contact",
          };
        }
    
        return {
          title: "不确定如何选择移液泵型号？",
          desc: "如果您不确定具体型号，可根据量程、吸头规格、液面检测方式、堵塞检测需求、通讯接口、安装空间和控制方式等信息联系我们。恒永达可协助您确认适合自动化仪器液体处理模块的移液泵配置。",
          button: "联系工程师确认",
          href: "/contact"
        };
      }
    return null;
    }
    

------------------------------------------------------------
行号：612
命中：function collectProductImageUrls(data: any) {

        return "";
      }
    
      if (/^https?:\/\//i.test(text)) {
        return text;
      }
    
      return `${PRODUCT_SITE_ORIGIN}${text.startsWith("/") ? text : `/${text}`}`;
    }
    
>>> function collectProductImageUrls(data: any) {
      const values: unknown[] = [
        data.mainImage,
        data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
        ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;

------------------------------------------------------------
行号：614
命中：data.mainImage,

    
      if (/^https?:\/\//i.test(text)) {
        return text;
      }
    
      return `${PRODUCT_SITE_ORIGIN}${text.startsWith("/") ? text : `/${text}`}`;
    }
    
    function collectProductImageUrls(data: any) {
      const values: unknown[] = [
>>>     data.mainImage,
        data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
        ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }

------------------------------------------------------------
行号：615
命中：data.imageUrl,

      if (/^https?:\/\//i.test(text)) {
        return text;
      }
    
      return `${PRODUCT_SITE_ORIGIN}${text.startsWith("/") ? text : `/${text}`}`;
    }
    
    function collectProductImageUrls(data: any) {
      const values: unknown[] = [
        data.mainImage,
>>>     data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
        ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }
    

------------------------------------------------------------
行号：617
命中：data.heroImage,

      }
    
      return `${PRODUCT_SITE_ORIGIN}${text.startsWith("/") ? text : `/${text}`}`;
    }
    
    function collectProductImageUrls(data: any) {
      const values: unknown[] = [
        data.mainImage,
        data.imageUrl,
        data.image,
>>>     data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
        ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }
    
          return [];
        })

------------------------------------------------------------
行号：620
命中：...(Array.isArray(data.galleryImages) ? data.galleryImages : []),

    }
    
    function collectProductImageUrls(data: any) {
      const values: unknown[] = [
        data.mainImage,
        data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
>>>     ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }
    
          return [];
        })
        .map(toAbsoluteProductUrl)
        .filter(Boolean);
    

------------------------------------------------------------
行号：637
命中：.filter(Boolean);

          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }
    
          return [];
        })
        .map(toAbsoluteProductUrl)
>>>     .filter(Boolean);
    
      return Array.from(new Set(urls));
    }
    
    function collectProductSpecifications(data: any) {
      const source = Array.isArray(data.specs)
        ? data.specs
        : Array.isArray(data.specificationGroups)
          ? data.specificationGroups
          : [];
      const rows: Array<{ name: string; value: string }> = [];
    
      function visit(value: unknown) {
        if (Array.isArray(value)) {
          value.forEach(visit);
          return;
        }
    

------------------------------------------------------------
行号：639
命中：return Array.from(new Set(urls));

          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;
            return [item.src, item.url, item.path, item.fullPath];
          }
    
          return [];
        })
        .map(toAbsoluteProductUrl)
        .filter(Boolean);
    
>>>   return Array.from(new Set(urls));
    }
    
    function collectProductSpecifications(data: any) {
      const source = Array.isArray(data.specs)
        ? data.specs
        : Array.isArray(data.specificationGroups)
          ? data.specificationGroups
          : [];
      const rows: Array<{ name: string; value: string }> = [];
    
      function visit(value: unknown) {
        if (Array.isArray(value)) {
          value.forEach(visit);
          return;
        }
    
        if (!value || typeof value !== "object") {
          return;

------------------------------------------------------------
行号：692
命中：const imageUrls = collectProductImageUrls(data);

        new Map(rows.map((row) => [`${row.name}::${row.value}`, row])).values()
      );
    }
    
    function buildProductStructuredData(data: any, pathname: string) {
      const canonicalUrl = toAbsoluteProductUrl(pathname || "/");
      const productName = String(
        data.model || data.title || data.name || ""
      ).trim();
      const description = String(data.description || "").trim();
>>>   const imageUrls = collectProductImageUrls(data);
      const applications = Array.isArray(data.commonApplications)
        ? data.commonApplications.filter(Boolean).map(String)
        : [];
      const specifications = collectProductSpecifications(data);
      const faqs: Array<{ question: string; answer: string }> = Array.isArray(data.faqs)
        ? data.faqs
            .map((item: any) => ({
              question: String(item?.question || "").trim(),
              answer: String(item?.answer || "").trim(),
            }))
            .filter((item: { question: string; answer: string }) =>
              item.question && item.answer
            )
        : [];
      const category = String(
        data.categoryLabel || data.productTypeName || ""
      ).trim();
    

------------------------------------------------------------
行号：694
命中：? data.commonApplications.filter(Boolean).map(String)

    }
    
    function buildProductStructuredData(data: any, pathname: string) {
      const canonicalUrl = toAbsoluteProductUrl(pathname || "/");
      const productName = String(
        data.model || data.title || data.name || ""
      ).trim();
      const description = String(data.description || "").trim();
      const imageUrls = collectProductImageUrls(data);
      const applications = Array.isArray(data.commonApplications)
>>>     ? data.commonApplications.filter(Boolean).map(String)
        : [];
      const specifications = collectProductSpecifications(data);
      const faqs: Array<{ question: string; answer: string }> = Array.isArray(data.faqs)
        ? data.faqs
            .map((item: any) => ({
              question: String(item?.question || "").trim(),
              answer: String(item?.answer || "").trim(),
            }))
            .filter((item: { question: string; answer: string }) =>
              item.question && item.answer
            )
        : [];
      const category = String(
        data.categoryLabel || data.productTypeName || ""
      ).trim();
    
      const product: Record<string, unknown> = {
        "@type": "Product",

------------------------------------------------------------
行号：736
命中：if (imageUrls.length > 0) {

      };
    
      if (data.productCode || data.seriesCode) {
        product.sku = String(data.productCode || data.seriesCode);
      }
    
      if (category) {
        product.category = category;
      }
    
>>>   if (imageUrls.length > 0) {
        product.image = imageUrls;
      }
    
      if (specifications.length > 0) {
        product.additionalProperty = specifications.map((item) => ({
          "@type": "PropertyValue",
          name: item.name,
          value: item.value,
        }));
      }
    
      const graph: Record<string, unknown>[] = [
        product,
        {
          "@type": "BreadcrumbList",
          "@id": `${canonicalUrl}#breadcrumb`,
          itemListElement: [
            {

------------------------------------------------------------
行号：737
命中：product.image = imageUrls;

    
      if (data.productCode || data.seriesCode) {
        product.sku = String(data.productCode || data.seriesCode);
      }
    
      if (category) {
        product.category = category;
      }
    
      if (imageUrls.length > 0) {
>>>     product.image = imageUrls;
      }
    
      if (specifications.length > 0) {
        product.additionalProperty = specifications.map((item) => ({
          "@type": "PropertyValue",
          name: item.name,
          value: item.value,
        }));
      }
    
      const graph: Record<string, unknown>[] = [
        product,
        {
          "@type": "BreadcrumbList",
          "@id": `${canonicalUrl}#breadcrumb`,
          itemListElement: [
            {
              "@type": "ListItem",

------------------------------------------------------------
行号：826
命中：thumbnails: "Product thumbnails",

            ? buildProductStructuredData(data, pathname || "/")
            : null,
        [data, isEnglish, pathname]
      );
      const copy = isEnglish
        ? {
            breadcrumb: "Breadcrumb navigation",
            home: "Home",
            products: "Products",
            gallery: "Product gallery",
>>>         thumbnails: "Product thumbnails",
            previous: "Previous image",
            next: "Next image",
            frontView: "Front view",
            sideView: "Side view",
            portDetail: "Port detail",
            applications: "Typical Applications:",
            model: "Model:",
            configurator: "Configure",
            datasheet: "Add Datasheet",
            drawing: "Add Drawing",
            drawingAdded: "Drawing Added",
            request3d: "Request 3D File",
            addToList: "Add to List",
            addedToList: "Added to List",
            tabs: "Product resources",
            specifications: "Specifications",
            model3d: "3D Model",
            technicalDrawing: "Technical Drawing",

------------------------------------------------------------
行号：862
命中：thumbnails: "缩略图区域",

            valved: "Valve Configuration",
            shape: "Shape",
            housingMaterial: "Housing Material",
            faq: "Frequently Asked Questions",
          }
        : {
            breadcrumb: "面包屑导航",
            home: "首页",
            products: "产品中心",
            gallery: "产品图片区域",
>>>         thumbnails: "缩略图区域",
            previous: "上一张",
            next: "下一张",
            frontView: "主视图",
            sideView: "侧视图",
            portDetail: "接口细节",
            applications: "常见应用：",
            model: "型号：",
            configurator: "配置选择",
            datasheet: "添加规格书",
            drawing: "添加图纸",
            drawingAdded: "已添加图纸",
            request3d: "申请3D文件",
            addToList: "加入清单",
            addedToList: "已加入清单",
            tabs: "产品资料切换",
            specifications: "规格",
            model3d: "3D模型",
            technicalDrawing: "零件图",

------------------------------------------------------------
行号：907
命中：if (data.mainImage) {

      const [activeThumb, setActiveThumb] = useState(0);
      const [isZooming, setIsZooming] = useState(false);
      const [zoomPosition, setZoomPosition] = useState({
        x: 50,
        y: 50,
      });
    
      const realImages = useMemo(() => {
        const images: string[] = [];
    
>>>     if (data.mainImage) {
          images.push(data.mainImage);
        }
    
        data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
      }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages
        ? realImages[Math.min(activeThumb, realImages.length - 1)]
        : null;
    
      /*

------------------------------------------------------------
行号：908
命中：images.push(data.mainImage);

      const [isZooming, setIsZooming] = useState(false);
      const [zoomPosition, setZoomPosition] = useState({
        x: 50,
        y: 50,
      });
    
      const realImages = useMemo(() => {
        const images: string[] = [];
    
        if (data.mainImage) {
>>>       images.push(data.mainImage);
        }
    
        data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
      }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages
        ? realImages[Math.min(activeThumb, realImages.length - 1)]
        : null;
    
      /*
       * 正式数据中：

------------------------------------------------------------
行号：911
命中：data.additionalImages.forEach((image) => {

        y: 50,
      });
    
      const realImages = useMemo(() => {
        const images: string[] = [];
    
        if (data.mainImage) {
          images.push(data.mainImage);
        }
    
>>>     data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
      }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages
        ? realImages[Math.min(activeThumb, realImages.length - 1)]
        : null;
    
      /*
       * 正式数据中：
       * 只有主图且没有附属图时，不显示缩略图栏。
       *
       * 当前测试数据尚未连接选型主图，因此保留 HTML 原型中的

------------------------------------------------------------
行号：918
命中：}, [data.additionalImages, data.mainImage]);

          images.push(data.mainImage);
        }
    
        data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
>>>   }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages
        ? realImages[Math.min(activeThumb, realImages.length - 1)]
        : null;
    
      /*
       * 正式数据中：
       * 只有主图且没有附属图时，不显示缩略图栏。
       *
       * 当前测试数据尚未连接选型主图，因此保留 HTML 原型中的
       * 三个 SVG 缩略图，便于核对版式。
       */
      const showThumbnailRow = hasRealImages
        ? realImages.length > 0
        : true;
      const zoomStyle: ZoomStyle = {
        "--zoom-x": `${zoomPosition.x}%`,

------------------------------------------------------------
行号：954
命中：function handleMainImageMove(event: MouseEvent<HTMLDivElement>) {

          ? runtimeDataForSectionTitle.sectionTitleMap
          : {};
    
      function getDbSectionTitle(sectionKey: string, fallback: string) {
        const value = sectionTitleMap[sectionKey];
    
        return typeof value === "string" && value.trim()
          ? value.trim()
          : fallback;
      }
>>>   function handleMainImageMove(event: MouseEvent<HTMLDivElement>) {
        const rect = event.currentTarget.getBoundingClientRect();
    
        const x = ((event.clientX - rect.left) / rect.width) * 100;
        const y = ((event.clientY - rect.top) / rect.height) * 100;
    
        setZoomPosition({
          x: Math.max(0, Math.min(100, x)),
          y: Math.max(0, Math.min(100, y)),
        });
      }
    
      function handlePreviousThumb() {
        if (hasRealImages) {
          setActiveThumb((current) =>
            current === 0 ? realImages.length - 1 : current - 1,
          );
          return;
        }

------------------------------------------------------------
行号：966
命中：function handlePreviousThumb() {

    
        const x = ((event.clientX - rect.left) / rect.width) * 100;
        const y = ((event.clientY - rect.top) / rect.height) * 100;
    
        setZoomPosition({
          x: Math.max(0, Math.min(100, x)),
          y: Math.max(0, Math.min(100, y)),
        });
      }
    
>>>   function handlePreviousThumb() {
        if (hasRealImages) {
          setActiveThumb((current) =>
            current === 0 ? realImages.length - 1 : current - 1,
          );
          return;
        }
    
        setActiveThumb((current) => (current === 0 ? 2 : current - 1));
      }
    
      function handleNextThumb() {
        if (hasRealImages) {
          setActiveThumb((current) =>
            current === realImages.length - 1 ? 0 : current + 1,
          );
          return;
        }
    

------------------------------------------------------------
行号：977
命中：function handleNextThumb() {

        if (hasRealImages) {
          setActiveThumb((current) =>
            current === 0 ? realImages.length - 1 : current - 1,
          );
          return;
        }
    
        setActiveThumb((current) => (current === 0 ? 2 : current - 1));
      }
    
>>>   function handleNextThumb() {
        if (hasRealImages) {
          setActiveThumb((current) =>
            current === realImages.length - 1 ? 0 : current + 1,
          );
          return;
        }
    
        setActiveThumb((current) => (current === 2 ? 0 : current + 1));
      }
    
      function getDetailCartProductCode() {
        return String(
          data.modelDisplay ||
            data.displayModel ||
            data.foreachModel ||
            data.model ||
            data.productCode ||
            data.slug ||

------------------------------------------------------------
行号：1054
命中：imagePath:

    
        return {
          sourceType: "pump-selection",
          sourceLabel: isEnglish ? "Product Detail Page" : "产品详情页",
          productName,
          productCode,
          foreachModel: modelText,
          competitorModels: [],
          quantity: 1,
          needDrawing,
>>>       imagePath:
            data.imageCard ||
            data.image ||
            data.imageUrl ||
            data.mainImage ||
            data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }
    
      
      const currentDetailCartItem = getItem("pump-selection", getDetailCartProductCode());
      const isDetailProductSelected = Boolean(currentDetailCartItem);

------------------------------------------------------------
行号：1055
命中：data.imageCard ||

        return {
          sourceType: "pump-selection",
          sourceLabel: isEnglish ? "Product Detail Page" : "产品详情页",
          productName,
          productCode,
          foreachModel: modelText,
          competitorModels: [],
          quantity: 1,
          needDrawing,
          imagePath:
>>>         data.imageCard ||
            data.image ||
            data.imageUrl ||
            data.mainImage ||
            data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }
    
      
      const currentDetailCartItem = getItem("pump-selection", getDetailCartProductCode());
      const isDetailProductSelected = Boolean(currentDetailCartItem);
      const isDetailDrawingSelected = Boolean(currentDetailCartItem?.needDrawing);

------------------------------------------------------------
行号：1057
命中：data.imageUrl ||

          sourceLabel: isEnglish ? "Product Detail Page" : "产品详情页",
          productName,
          productCode,
          foreachModel: modelText,
          competitorModels: [],
          quantity: 1,
          needDrawing,
          imagePath:
            data.imageCard ||
            data.image ||
>>>         data.imageUrl ||
            data.mainImage ||
            data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }
    
      
      const currentDetailCartItem = getItem("pump-selection", getDetailCartProductCode());
      const isDetailProductSelected = Boolean(currentDetailCartItem);
      const isDetailDrawingSelected = Boolean(currentDetailCartItem?.needDrawing);
    
      function handleOpenConfigurator() {

------------------------------------------------------------
行号：1058
命中：data.mainImage ||

          productName,
          productCode,
          foreachModel: modelText,
          competitorModels: [],
          quantity: 1,
          needDrawing,
          imagePath:
            data.imageCard ||
            data.image ||
            data.imageUrl ||
>>>         data.mainImage ||
            data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }
    
      
      const currentDetailCartItem = getItem("pump-selection", getDetailCartProductCode());
      const isDetailProductSelected = Boolean(currentDetailCartItem);
      const isDetailDrawingSelected = Boolean(currentDetailCartItem?.needDrawing);
    
      function handleOpenConfigurator() {
        console.info("配置选择端口预留", data.slug);

------------------------------------------------------------
行号：1059
命中：data.heroImage ||

          productCode,
          foreachModel: modelText,
          competitorModels: [],
          quantity: 1,
          needDrawing,
          imagePath:
            data.imageCard ||
            data.image ||
            data.imageUrl ||
            data.mainImage ||
>>>         data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }
    
      
      const currentDetailCartItem = getItem("pump-selection", getDetailCartProductCode());
      const isDetailProductSelected = Boolean(currentDetailCartItem);
      const isDetailDrawingSelected = Boolean(currentDetailCartItem?.needDrawing);
    
      function handleOpenConfigurator() {
        console.info("配置选择端口预留", data.slug);
      }

------------------------------------------------------------
行号：1156
命中：styles.mainImage,

            },
          ]}
        >
          <main className={styles.page} data-product-detail-page="true">
          <div className={styles.container}>
            
            <section className={styles.productTop}>
              <div data-product-gallery="true" className={styles.gallery} aria-label={copy.gallery}>
                <div data-product-main-stage="true"
                  className={[
>>>                 styles.mainImage,
                    isZooming ? styles.isZooming : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
                  onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
                      alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"

------------------------------------------------------------
行号：1159
命中：.filter(Boolean)

          <main className={styles.page} data-product-detail-page="true">
          <div className={styles.container}>
            
            <section className={styles.productTop}>
              <div data-product-gallery="true" className={styles.gallery} aria-label={copy.gallery}>
                <div data-product-main-stage="true"
                  className={[
                    styles.mainImage,
                    isZooming ? styles.isZooming : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
                  onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
                      alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"
                      aria-hidden="true"
                    >
                      <g transform="translate(34, 84)">

------------------------------------------------------------
行号：1164
命中：onMouseMove={handleMainImageMove}

                <div data-product-main-stage="true"
                  className={[
                    styles.mainImage,
                    isZooming ? styles.isZooming : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
>>>               onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
                      alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"
                      aria-hidden="true"
                    >
                      <g transform="translate(34, 84)">
                        <rect
                          x="0"
                          y="52"
                          width="54"
                          height="20"

------------------------------------------------------------
行号：1169
命中：alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}

                    .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
                  onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
>>>                   alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"
                      aria-hidden="true"
                    >
                      <g transform="translate(34, 84)">
                        <rect
                          x="0"
                          y="52"
                          width="54"
                          height="20"
                          fill="#ffffff"
                          stroke="#d7dee8"
                          strokeWidth="2"
                        />
                        <rect

------------------------------------------------------------
行号：1272
命中：<div data-product-thumb-row="true" className={styles.thumbRow} aria-label={copy.thumbnails}>

                          y2="100"
                          stroke="#d7dee8"
                          strokeWidth="4"
                        />
                      </g>
                    </svg>
                  )}
                </div>
    
                {showThumbnailRow ? (
>>>               <div data-product-thumb-row="true" className={styles.thumbRow} aria-label={copy.thumbnails}>
                    <button
                      className={styles.thumbArrow}
                      type="button"
                      aria-label={copy.previous}
                      onClick={handlePreviousThumb}
                    >
                      ‹
                    </button>
    
                    {hasRealImages ? (
                      realImages.slice(0, 5).map((image, index) => (
                        <button
                          key={`${image}-${index}`}
                          className={[
                            styles.thumb,
                            activeThumb === index ? styles.isActive : "",
                          ]
                            .filter(Boolean)

------------------------------------------------------------
行号：1277
命中：onClick={handlePreviousThumb}

                    </svg>
                  )}
                </div>
    
                {showThumbnailRow ? (
                  <div data-product-thumb-row="true" className={styles.thumbRow} aria-label={copy.thumbnails}>
                    <button
                      className={styles.thumbArrow}
                      type="button"
                      aria-label={copy.previous}
>>>                   onClick={handlePreviousThumb}
                    >
                      ‹
                    </button>
    
                    {hasRealImages ? (
                      realImages.slice(0, 5).map((image, index) => (
                        <button
                          key={`${image}-${index}`}
                          className={[
                            styles.thumb,
                            activeThumb === index ? styles.isActive : "",
                          ]
                            .filter(Boolean)
                            .join(" ")}
                          type="button"
                          onClick={() => setActiveThumb(index)}
                        >
                          <img src={image} alt="" />

------------------------------------------------------------
行号：1290
命中：.filter(Boolean)

                    </button>
    
                    {hasRealImages ? (
                      realImages.slice(0, 5).map((image, index) => (
                        <button
                          key={`${image}-${index}`}
                          className={[
                            styles.thumb,
                            activeThumb === index ? styles.isActive : "",
                          ]
>>>                         .filter(Boolean)
                            .join(" ")}
                          type="button"
                          onClick={() => setActiveThumb(index)}
                        >
                          <img src={image} alt="" />
                        </button>
                      ))
                    ) : (
                      <>
                        <button
                          className={[
                            styles.thumb,
                            activeThumb === 0 ? styles.isActive : "",
                          ]
                            .filter(Boolean)
                            .join(" ")}
                          type="button"
                          aria-label={copy.frontView}

------------------------------------------------------------
行号：1305
命中：.filter(Boolean)

                          <img src={image} alt="" />
                        </button>
                      ))
                    ) : (
                      <>
                        <button
                          className={[
                            styles.thumb,
                            activeThumb === 0 ? styles.isActive : "",
                          ]
>>>                         .filter(Boolean)
                            .join(" ")}
                          type="button"
                          aria-label={copy.frontView}
                          onClick={() => setActiveThumb(0)}
                        >
                          <svg viewBox="0 0 120 70" aria-hidden="true">
                            <rect
                              x="18"
                              y="28"
                              width="84"
                              height="16"
                              fill="#ffffff"
                              stroke="#d7dee8"
                              strokeWidth="2"
                            />
                            <path
                              d="M72 20 L94 20 L104 30 L104 40 L94 50 L72 50 Z"
                              fill="#173368"

------------------------------------------------------------
行号：1333
命中：.filter(Boolean)

                              fill="#173368"
                            />
                          </svg>
                        </button>
    
                        <button
                          className={[
                            styles.thumb,
                            activeThumb === 1 ? styles.isActive : "",
                          ]
>>>                         .filter(Boolean)
                            .join(" ")}
                          type="button"
                          aria-label={copy.sideView}
                          onClick={() => setActiveThumb(1)}
                        >
                          <svg viewBox="0 0 120 70" aria-hidden="true">
                            <rect
                              x="26"
                              y="19"
                              width="54"
                              height="32"
                              fill="#ffffff"
                              stroke="#d7dee8"
                              strokeWidth="2"
                            />
                            <rect
                              x="80"
                              y="15"

------------------------------------------------------------
行号：1364
命中：.filter(Boolean)

                              fill="#173368"
                            />
                          </svg>
                        </button>
    
                        <button
                          className={[
                            styles.thumb,
                            activeThumb === 2 ? styles.isActive : "",
                          ]
>>>                         .filter(Boolean)
                            .join(" ")}
                          type="button"
                          aria-label={copy.portDetail}
                          onClick={() => setActiveThumb(2)}
                        >
                          <svg viewBox="0 0 120 70" aria-hidden="true">
                            <circle
                              cx="60"
                              cy="35"
                              r="22"
                              fill="#ffffff"
                              stroke="#d7dee8"
                              strokeWidth="2"
                            />
                            <circle
                              cx="60"
                              cy="35"
                              r="7"

------------------------------------------------------------
行号：1394
命中：onClick={handleNextThumb}

                            />
                          </svg>
                        </button>
                      </>
                    )}
    
                    <button
                      className={styles.thumbArrow}
                      type="button"
                      aria-label={copy.next}
>>>                   onClick={handleNextThumb}
                    >
                      ›
                    </button>
                  </div>
                ) : null}
              </div>
    
              <div className={styles.productInfo}>
                <div className={styles.titleGroup}>
                  <h1 className={styles.productModelTitle}>{data.model}</h1>
                </div>
    
                <p className={styles.productDesc}>
                  {(data as any).description || (Array.isArray(data.advantages) ? data.advantages.join("") : "")}
                </p>
    
                <div className={styles.application}>
                  <p className={styles.applicationTitle}>

------------------------------------------------------------
行号：1513
命中：.filter(Boolean)

              </div>
            </section>
    
            <section className={styles.detailSection}>
              <nav className={styles.tabNav} aria-label={copy.tabs}>
                <button
                  className={[
                    styles.tabButton,
                    activeTab === "spec" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                  type="button"
                  onClick={() => setActiveTab("spec")}
                >
                  {copy.specifications}
                </button>
    
                <button
                  className={[
                    styles.tabButton,
                    activeTab === "model3d" ? styles.isActive : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                  type="button"
                  onClick={() => setActiveTab("model3d")}
                >
                  {copy.model3d}

------------------------------------------------------------
行号：1526
命中：.filter(Boolean)

                  onClick={() => setActiveTab("spec")}
                >
                  {copy.specifications}
                </button>
    
                <button
                  className={[
                    styles.tabButton,
                    activeTab === "model3d" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                  type="button"
                  onClick={() => setActiveTab("model3d")}
                >
                  {copy.model3d}
                </button>
    
                <button
                  className={[
                    styles.tabButton,
                    activeTab === "drawing" ? styles.isActive : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                  type="button"
                  onClick={() => setActiveTab("drawing")}
                >
                  {copy.technicalDrawing}

------------------------------------------------------------
行号：1539
命中：.filter(Boolean)

                  onClick={() => setActiveTab("model3d")}
                >
                  {copy.model3d}
                </button>
    
                <button
                  className={[
                    styles.tabButton,
                    activeTab === "drawing" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                  type="button"
                  onClick={() => setActiveTab("drawing")}
                >
                  {copy.technicalDrawing}
                </button>
              </nav>
    
              <div className={styles.panelWrap}>
                <div
                  className={[
                    styles.panel,
                    activeTab === "spec" ? styles.isActive : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                >
                  <div

------------------------------------------------------------
行号：1554
命中：.filter(Boolean)

                  {copy.technicalDrawing}
                </button>
              </nav>
    
              <div className={styles.panelWrap}>
                <div
                  className={[
                    styles.panel,
                    activeTab === "spec" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                >
                  <div
                    className={[
                      styles.panelBox,
                      styles.specPanelClean,
                    ].join(" ")}
                  >
                    <table className={styles.specTable}>
                      <tbody>
                        {data.specs.map((item) => (
                          <tr data-product-spec-row="true" key={`${item.label}-${item.value}`}>
                            <th>{item.label}</th>
                            <td>{item.value}</td>
                          </tr>
                        ))}
                      </tbody>
                    </table>

------------------------------------------------------------
行号：1581
命中：.filter(Boolean)

                      </tbody>
                    </table>
                  </div>
                </div>
    
                <div
                  className={[
                    styles.panel,
                    activeTab === "model3d" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                >
                  <div
                      className={styles.panelBox}
                      data-product-model3d-panel="true"
                    >
                    <ProductModelViewer
                      slug={data.slug}
                      modelName={data.model}
                      modelUrl={(data as any).model3dUrl || (data as any).resources?.model3dUrl}
                      locale={isEnglish ? "en" : "zh"}
                    />
                  </div>
                </div>
    
                <div
                  className={[
                    styles.panel,

------------------------------------------------------------
行号：1602
命中：.filter(Boolean)

                      locale={isEnglish ? "en" : "zh"}
                    />
                  </div>
                </div>
    
                <div
                  className={[
                    styles.panel,
                    activeTab === "drawing" ? styles.isActive : "",
                  ]
>>>                 .filter(Boolean)
                    .join(" ")}
                >
                  {getProductDrawingPreviewUrl(data.slug, (data as any).drawing2dUrl || (data as any).drawingPdfUrl || (data as any).partDrawingUrl || (data as any).resources?.drawing2dUrl) ? (
                    <PdfDrawingPreview
                      pdfPreviewUrl={getProductDrawingPreviewUrl(data.slug, (data as any).drawing2dUrl || (data as any).drawingPdfUrl || (data as any).partDrawingUrl || (data as any).resources?.drawing2dUrl)}
                      documentTitle={data.model}
                      text={
                        isEnglish
                          ? {
                              title: "Technical Drawing",
                              loadingLabel: "Loading drawing...",
                              previewButton: "Preview Drawing",
                              description: `View the technical drawing for ${data.model}.`,
                            }
                          : undefined
                      }
                    />
                  ) : (

------------------------------------------------------------
行号：1717
命中：.filter(Boolean)

                <div className={styles.faqList}>
                  {data.faqs.map((item, index) => {
                    const isOpen = openFaqIndex === index;
    
                    return (
                      <article
                        className={[
                          styles.faqItem,
                          isOpen ? styles.faqItemOpen : "",
                        ]
>>>                       .filter(Boolean)
                          .join(" ")}
                        key={`${item.question}-${index}`}
                      >
                        <button
                          className={styles.faqQuestion}
                          type="button"
                          aria-expanded={isOpen}
                          onClick={() =>
                            setOpenFaqIndex(isOpen ? null : index)
                          }
                        >
                          <span className={styles.faqQuestionText}>
                            {item.question}
                          </span>
                          <span
                            className={styles.faqToggle}
                            aria-hidden="true"
                          >

============================================================
二、可能的英文详情数据适配器
============================================================

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 28 行：import { localizeProductDetailData } from "@/data/products/detail/product-detail.intl";
    import PdfDrawingPreview from "@/components/common/PdfDrawingPreview";
    import { usePathname } from "next/navigation";
    import { useMemo, useState } from "react";
    
    import type { CSSProperties, MouseEvent } from "react";
>>> import { localizeProductDetailData } from "@/data/products/detail/product-detail.intl";
    import type { ProductDetailPageData } from "@/data/products/detail/product-detail.types";
    import ProductModelViewer from "./ProductModelViewer";
    import styles from "./product-detail.module.css";
    
    type ProductDetailTab = "spec" | "model3d" | "drawing";
    
    type ProductDetailClientProps = {
      data: ProductDetailPageData & Record<string, any>;
    };
    

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 614 行：data.mainImage,
      return `${PRODUCT_SITE_ORIGIN}${text.startsWith("/") ? text : `/${text}`}`;
    }
    
    function collectProductImageUrls(data: any) {
      const values: unknown[] = [
>>>     data.mainImage,
        data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
        ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 620 行：...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
        data.imageUrl,
        data.image,
        data.heroImage,
        data.coverImage,
        ...(Array.isArray(data.images) ? data.images : []),
>>>     ...(Array.isArray(data.galleryImages) ? data.galleryImages : []),
      ];
    
      const urls = values
        .flatMap((value) => {
          if (typeof value === "string") {
            return [value];
          }
    
          if (value && typeof value === "object") {
            const item = value as Record<string, unknown>;

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 809 行：? localizeProductDetailData(sourceData)
      const pathname = usePathname();
      const isEnglish = pathname === "/en" || pathname.startsWith("/en/");
      const data = useMemo(
        () =>
          isEnglish
>>>         ? localizeProductDetailData(sourceData)
            : sourceData,
        [isEnglish, sourceData]
      );
      const structuredData = useMemo(
        () =>
          isEnglish
            ? buildProductStructuredData(data, pathname || "/")
            : null,
        [data, isEnglish, pathname]
      );

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 907 行：if (data.mainImage) {
      });
    
      const realImages = useMemo(() => {
        const images: string[] = [];
    
>>>     if (data.mainImage) {
          images.push(data.mainImage);
        }
    
        data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 908 行：images.push(data.mainImage);
    
      const realImages = useMemo(() => {
        const images: string[] = [];
    
        if (data.mainImage) {
>>>       images.push(data.mainImage);
        }
    
        data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
      }, [data.additionalImages, data.mainImage]);

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 911 行：data.additionalImages.forEach((image) => {
    
        if (data.mainImage) {
          images.push(data.mainImage);
        }
    
>>>     data.additionalImages.forEach((image) => {
          if (image && !images.includes(image)) {
            images.push(image);
          }
        });
    
        return images;
      }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 918 行：}, [data.additionalImages, data.mainImage]);
            images.push(image);
          }
        });
    
        return images;
>>>   }, [data.additionalImages, data.mainImage]);
    
      const hasRealImages = realImages.length > 0;
      const activeRealImage = hasRealImages
        ? realImages[Math.min(activeThumb, realImages.length - 1)]
        : null;
    
      /*
       * 正式数据中：
       * 只有主图且没有附属图时，不显示缩略图栏。
       *

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 954 行：function handleMainImageMove(event: MouseEvent<HTMLDivElement>) {
    
        return typeof value === "string" && value.trim()
          ? value.trim()
          : fallback;
      }
>>>   function handleMainImageMove(event: MouseEvent<HTMLDivElement>) {
        const rect = event.currentTarget.getBoundingClientRect();
    
        const x = ((event.clientX - rect.left) / rect.width) * 100;
        const y = ((event.clientY - rect.top) / rect.height) * 100;
    
        setZoomPosition({
          x: Math.max(0, Math.min(100, x)),
          y: Math.max(0, Math.min(100, y)),
        });
      }

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 1058 行：data.mainImage ||
          needDrawing,
          imagePath:
            data.imageCard ||
            data.image ||
            data.imageUrl ||
>>>         data.mainImage ||
            data.heroImage ||
            "",
          detailHref:
            data.detailHref ||
            data.href ||
            (isEnglish && fallbackDetailHref
              ? `/en${fallbackDetailHref}`
              : fallbackDetailHref),
        };
      }

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 1156 行：styles.mainImage,
            
            <section className={styles.productTop}>
              <div data-product-gallery="true" className={styles.gallery} aria-label={copy.gallery}>
                <div data-product-main-stage="true"
                  className={[
>>>                 styles.mainImage,
                    isZooming ? styles.isZooming : "",
                  ]
                    .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
                  onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 1164 行：onMouseMove={handleMainImageMove}
                    .filter(Boolean)
                    .join(" ")}
                  style={zoomStyle}
                  onMouseEnter={() => setIsZooming(true)}
                  onMouseLeave={() => setIsZooming(false)}
>>>               onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
                      alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"

------------------------------------------------------------
文件：components\products\detail\ProductDetailClient.tsx
第 1169 行：alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                  onMouseMove={handleMainImageMove}
                >
                  {activeRealImage ? (
                    <img data-product-main-image="true"
                      src={activeRealImage}
>>>                   alt={(data as any).imageAltEn || (data as any).mainImageAlt || (data as any).imageAlt || `${data.model} ${data.name}`}
                    />
                  ) : (
                    <svg
                      className={styles.pumpSvg}
                      viewBox="0 0 520 310"
                      aria-hidden="true"
                    >
                      <g transform="translate(34, 84)">
                        <rect
                          x="0"

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 764 行：function normalizeDetailPathPart(value: unknown) {
      });
    
      return openGroups;
    }
    
>>> function normalizeDetailPathPart(value: unknown) {
      return String(value || "")
        .trim()
        .replace(/^\/+|\/+$/g, "")
        .split("/")
        .filter(Boolean)
        .pop() || "";
    }
    
    function normalizeModelKey(value: unknown) {
      return String(value || "")

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 824 行：return normalizeDetailPathPart(
          .filter(Boolean);
    
        return detailCandidates.some((item) => candidates.includes(item));
      });
    
>>>   return normalizeDetailPathPart(
        matchedDetail?.slug ||
          matchedDetail?.detailSlug ||
          product.detailSlug
      );
    }
    
    
    /* ===== FOREACH plunger pump model detail href helpers START ===== */
    
    function cleanPlungerHrefText(value: unknown) {

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 1169 行：normalizeDetailPathPart(
        if (matchedHref) {
          return matchedHref;
        }
    
        const matchedSlug =
>>>       normalizeDetailPathPart(
            matchedDetail?.slug ||
            matchedDetail?.detailSlug ||
            ""
          );
    
        if (matchedSlug) {
          return `/products/fittings/hard-tube-fittings/${matchedSlug}`;
        }
    
        /*

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 3284 行：localizeProductDetailHref(productTypeHref)
          productTypeId
        );
    
        if (productTypeHref) {
          router.push(
>>>         localizeProductDetailHref(productTypeHref)
          );
          return;
        }
    
        setActiveProductTypeId(productTypeId);
        setSelectedFilters(getDefaultSelectedFilters(activeCategoryId, productTypeId));
        setMobileOpenFilterGroups(getDefaultMobileOpenFilterGroups(productTypeId));
      }
    
      function handleFilterChange(

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 3368 行：? localizeProductDetailHref(productTypeHref)
           * 2. 取消系列时，回到二级产品类型 URL
           * 3. 使用 pushState 不触发 Next 路由跳转，页面不会明显跳动
           */
          const nextHref = isAlreadySelected
            ? productTypeHref
>>>           ? localizeProductDetailHref(productTypeHref)
              : productTypeHref
            : localizeProductDetailHref(seriesHref);
    
          if (nextHref) {
            const normalizedHref = nextHref.endsWith("/")
              ? nextHref
              : `${nextHref}/`;
    
            window.history.pushState(null, "", normalizedHref);
          }

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 3370 行：: localizeProductDetailHref(seriesHref);
           */
          const nextHref = isAlreadySelected
            ? productTypeHref
              ? localizeProductDetailHref(productTypeHref)
              : productTypeHref
>>>         : localizeProductDetailHref(seriesHref);
    
          if (nextHref) {
            const normalizedHref = nextHref.endsWith("/")
              ? nextHref
              : `${nextHref}/`;
    
            window.history.pushState(null, "", normalizedHref);
          }
    
          return;

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 4192 行：localizeProductDetailHref(productTypeHref)
            activeProductTypeId
          );
    
          if (productTypeHref) {
            router.push(
>>>           localizeProductDetailHref(productTypeHref)
            );
            return;
          }
        }
    
        /*
         * 说明：
         * selectedFilters 的 key 只能是 SelectionFilterKey。
         * removeSelectedTag 传进来的 key 是 string，
         * 所以这里必须先判断 key 是否属于 FILTER_KEYS，再转换类型。

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 4237 行：function localizeProductDetailHref(href: string) {
        setSelectedFilters(getDefaultSelectedFilters(activeCategoryId, firstProductTypeId));
        setSearchKeyword("");
        setMobileOpenFilterGroups(getDefaultMobileOpenFilterGroups(firstProductTypeId));
      }
    
>>>   function localizeProductDetailHref(href: string) {
        if (
          locale !== "en" ||
          !href.startsWith("/") ||
          href.startsWith(`/${locale}/`) ||
          href === `/${locale}`
        ) {
          return href;
        }
    
        return `/${locale}${href}`;

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 4297 行：detailHref: localizeProductDetailHref(makeDetailHref(product)),
              return "/images/products/pumps/diaphragm-pump.jpg";
            }
    
            return undefined;
          })(),
>>>       detailHref: localizeProductDetailHref(makeDetailHref(product)),
        };
      }
    
      function toggleProductInList(product: ProductSelectionProduct) {
        const currentItem = getItem("pump-selection", product.productId);
    
        if (currentItem) {
          removeItem(currentItem.id);
          return;
        }

------------------------------------------------------------
文件：components\products\selection\ProductSelectionClient.tsx
第 4437 行：localizeProductDetailHref(
                        }
                        getSubtitle={(product) =>
                          getText(locale, product.cardSubtitle, "")
                        }
                        getDetailHref={(product) =>
>>>                       localizeProductDetailHref(
                            normalizeFinalProductDetailHref(
                              product,
                              makeDetailHref(product)
                            )
                          )
                        }
                        onToggleList={toggleProductInList}
                      />
    
                      <ProductSelectionPagination

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 24 行：mainImage: string;
      name: string;
      capacity: number;
      material: EaProductMaterial;
      advantages: string[];
      commonApplications: string[];
>>>   mainImage: string;
      additionalImages: string[];
      showConfiguration: boolean;
      showDatasheetRequest: boolean;
      show2DRequest: boolean;
      show3DRequest: boolean;
      faqSeries: string;
    };
    
    export const eaProductDetailsZh = [
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 25 行：additionalImages: string[];
      capacity: number;
      material: EaProductMaterial;
      advantages: string[];
      commonApplications: string[];
      mainImage: string;
>>>   additionalImages: string[];
      showConfiguration: boolean;
      showDatasheetRequest: boolean;
      show2DRequest: boolean;
      show3DRequest: boolean;
      faqSeries: string;
    };
    
    export const eaProductDetailsZh = [
        {
            "category":  "pumps",

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 55 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 56 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 87 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 88 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-100ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 119 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 120 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 151 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 152 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-250ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 183 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 184 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 215 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 216 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-500ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 247 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 248 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 279 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 280 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-1000ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 311 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 312 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 343 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 344 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-2500ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 375 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 376 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 407 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 408 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-5000ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 439 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-pmma.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-pmma.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 440 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-pmma.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        },
        {

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 471 行："mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-peek.webp",
                                       "IVD诊断设备",
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
>>>         "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-peek.webp",
            "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        }

------------------------------------------------------------
文件：data\products\detail\ea-product-details.zh.generated.ts
第 472 行："additionalImages":  [
                                       "生命科学仪器",
                                       "实验室自动化",
                                       "分析仪器"
                                   ],
            "mainImage":  "/images/products/pumps/plunger-pump/ea/pump-ea-10000ul-peek.webp",
>>>         "additionalImages":  [
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-duanzi.webp",
                                     "/images/products/pumps/plunger-pump/ea/pump-ea-guangou.webp"
                                 ],
            "showConfiguration":  true,
            "showDatasheetRequest":  true,
            "show2DRequest":  true,
            "show3DRequest":  true,
            "faqSeries":  "EA常规柱塞泵"
        }
    ] as EaProductDetailZh[];

------------------------------------------------------------
文件：data\products\detail\getQuickConnectSeriesDetailData.ts
第 590 行：mainImage:
          "IVD设备",
          "分析仪器",
          "实验室自动化",
        ],
    
>>>     mainImage:
          images[0],
    
        image:
          images[0],
    
        heroImage:
          images[0],
    
        imageCard:
          images[0],

------------------------------------------------------------
文件：data\products\detail\getQuickConnectSeriesDetailData.ts
第 602 行：additionalImages:
          images[0],
    
        imageCard:
          images[0],
    
>>>     additionalImages:
          images.slice(1),
    
        imageAlt:
          `${series}快插接头系列`,
    
        mainImageAlt:
          `${series}快插接头系列`,
    
        detailMode:
          "selection",

------------------------------------------------------------
文件：data\products\detail\getQuickConnectSeriesDetailData.ts
第 608 行：mainImageAlt:
          images.slice(1),
    
        imageAlt:
          `${series}快插接头系列`,
    
>>>     mainImageAlt:
          `${series}快插接头系列`,
    
        detailMode:
          "selection",
    
        hideModelAction:
          false,
    
        showConfigurator:
          true,

------------------------------------------------------------
文件：data\products\detail\product-detail.intl.ts
第 3874 行：export function localizeProductDetailData<T extends DetailRecord>(data: T): T {
          localizeValue(childValue, childKey, context),
        ])
      );
    }
    
>>> export function localizeProductDetailData<T extends DetailRecord>(data: T): T {
      const productName = inferProductName(data);
      const model = inferModel(data, productName);
      const context = { model, productName };
      const localized = localizeValue(data, "root", context) as DetailRecord;
    
      /*
       * Specs are translated from the original Chinese rows after the generic
       * object pass. This prevents unknown labels and values from falling back
       * to the product name or losing their technical meaning.
       */

------------------------------------------------------------
文件：data\products\detail\product-detail.types.ts
第 46 行：additionalImages: string[];
      /**
       * 详情页附属图片。
       * 主图不在这里维护，后续从选型页面基础数据读取。
       * 没有附属图时使用空数组。
       */
>>>   additionalImages: string[];
    
      showConfigurator: boolean;
      showDatasheetRequest: boolean;
      showDrawingRequest: boolean;
      show3DRequest: boolean;
    
      /**
       * FAQ 第一版只预留，不渲染。
       */
      faqKey?: string;

------------------------------------------------------------
文件：data\products\detail\product-detail.types.ts
第 74 行：mainImage: string | null;
    
    export type ProductDetailPageData = ProductDetailZhRecord & {
      /**
       * 后续从选型页面基础数据读取。
       */
>>>   mainImage: string | null;
    
      /**
       * 后续由产品系列规格 Excel 生成。
       */
      specs: ProductSpecItem[];
    };

------------------------------------------------------------
文件：data\products\detail\product-detail.zh.ts
第 36 行：additionalImages: [],
    
        /*
         * 这里只维护详情页附属图片。
         * 没有附属图片时保持空数组。
         */
>>>     additionalImages: [],
    
        showConfigurator: true,
        showDatasheetRequest: true,
        showDrawingRequest: true,
        show3DRequest: true,
    
        /*
         * 第一版只预留 FAQ，不在页面显示。
         */
        faqKey: "ea-conventional-plunger-pump",

============================================================
三、示例产品图片字段检查
============================================================

mainImage: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg
image: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg
imagePath: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg
imageUrl: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg
heroImage: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg
imageCard: /images/products/fittings/filters/products/f-pe-100-32-pp-n-main.jpg

图片字段总数：6
实际唯一图片数：1

确认：多个图片字段实际指向同一张图片。英文适配时必须去重。

============================================================
需要确认的修复位置
============================================================

1. 英文路由是否把 mainImage、image、imagePath、imageUrl、heroImage、imageCard 全部组成图库。
2. 图片数组是否只使用 filter(Boolean)，但没有使用 Set 去重。
3. ProductDetailClient 是否根据数组 length > 1 显示左右按钮。
4. 中文路由传入的 images 为什么只有一项，而英文路由存在重复项。
5. 最终应在英文数据适配层统一去重，而不是逐个产品修改。

